#!/usr/bin/env node
import assert from'node:assert/strict';import fs from'node:fs';const r=p=>JSON.parse(fs.readFileSync(p,'utf8')),w=r('data/project/security-state-work-packages.json'),p=r('data/project/security-state-organism-program.json'),m=r('build/core-thesis/security-state-organism/manifest.json');assert.equal(w.packages.length,121);assert.deepEqual(m.counts.package_classes,{cross_estate_bridge:18,entity:41,estate:24,lineage_stage:8,organ:15,organism_test:8,theater:7});assert.equal(p.lineage_stages.length,8);assert.equal(p.organ_types.length,15);assert.equal(p.organism_tests.length,8);assert.equal(p.theaters.length,7);for(const x of w.packages){assert.equal(x.boundaries.graph_effect,'none');assert.equal(x.boundaries.conclusion_generated,false);assert.equal(x.boundaries.estate_completion_claimed,false);assert.ok(x.falsifier);assert.ok(x.terminal_states.includes('falsified'))}console.log('security-state-organism.test: ok');