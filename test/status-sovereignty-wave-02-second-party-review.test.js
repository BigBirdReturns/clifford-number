#!/usr/bin/env node
import assert from 'node:assert/strict';import {loadW02SPR,validateW02SPR} from '../tools/validate-status-sovereignty-wave-02-second-party-review.mjs';const base=loadW02SPR();assert.deepEqual(validateW02SPR(base),[]);const cases=[c=>c.campaign.campaign_id='X',c=>c.campaign.wave_id='X',c=>c.campaign.status='complete',c=>c.campaign.parent_review.release_sha256='0'.repeat(64),c=>c.campaign.counts.wave_packets=7,c=>c.campaign.counts.maintainer_reviewed_packets=7,...['reviewer_candidates','invitations','nonresponses','refusals','conflicts','ineligible','accepted_assignments','submitted_unvalidated','valid_reviews','second_party_reviewed_packets','adjudicated_packets','canonical_disposition_changes','publication_clearances','graph_effects'].map(k=>c=>c.campaign.counts[k]=1),c=>c.packets.packets.pop(),c=>c.packets.packets[1].packet_id=c.packets.packets[0].packet_id,c=>c.packets.packets[1].observation_id=c.packets.packets[0].observation_id,c=>c.packets.packets[0].assignment_state='accepted',c=>c.packets.packets[0].valid_review_count=1,c=>c.packets.packets[0].adjudication_state='complete',c=>c.packets.packets[0].review_packet_sha256='x',c=>c.packets.packets[0].parent_review_release_sha256='x',c=>c.review.counts.maintainer_reviewed=7,c=>c.candidates.records.push({}),c=>c.candidates.failure_denominator.push({}),c=>c.responses.records.push({}),c=>c.responses.failure_denominator.push({}),c=>c.campaign.boundaries.candidate_is_review=true,c=>c.campaign.boundaries.valid_review_rewrites_disposition=true,c=>c.campaign.boundaries.valid_review_clears_publication=true,c=>c.campaign.boundaries.valid_review_creates_graph_edge=true,c=>c.manifest.combined_sha256='0'.repeat(64),c=>c.buildManifest.combined_sha256='0'.repeat(64),c=>c.buildReport.status='x',c=>c.publicReport.release_manifest.combined_sha256='0'.repeat(64),c=>c.html='x'];for(const mutate of cases){const c=structuredClone(base);mutate(c);assert.ok(validateW02SPR(c).length>0)}console.log(`status-sovereignty-wave-02-second-party-review.test: ${cases.length} adversarial mutations PASS`);
