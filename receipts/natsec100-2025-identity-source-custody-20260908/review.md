# NatSec100 2025 identity-source custody

**Status:** source custody complete; case-receipt admission and identity promotion remain pending

**Observation date:** 2026-09-08
**Base:** `e5a02fa614db4d0f54c44acfb537dfcf53b5a716`

This packet executes the next bounded Phase 1 operation against every external identity source cited by the 23 unresolved 2025 NatSec100 adjudication rows. It retains all 26 requested source bodies, public header copies, redirect and transport receipts, and verified TLS leaf certificates for the 24 terminal hosts. Every request completed once with HTTP 200, curl TLS verification result zero, an allowed terminal host, identity or absent content coding, and textual content without a recognized challenge page.

The 19 exact brand/domain candidates have captured first-party support at the brand/domain tier. The two proposed existing-registry amendments, Second Front and OPAQUE, have captured support for the source alias and current domain. The two successor-brand candidates retain narrower continuity support: `aetherflux.com` redirected through `https://cowboyspace.com` to `https://www.cowboyspace.com/`, while the retained historical Aetherflux release and current Cowboy Space page preserve both brand endpoints; Axion’s current site retains an issuer article using Axion Ray and a legal notice identifying Axion Ray Inc. as “Axion.” These observations do not establish corporate legal succession.

The captured bodies total 8,754,182 bytes. The deterministic source archive contains 134 members and is 3,841,394 bytes with SHA-256 `fd6f3ff77e2314bfd12cdd34de1f120b552d86c4a27d021e2bcb1bceb87a8149`. Fourteen server-issued `Set-Cookie` values were removed from the public header copies and replaced with one-way SHA-256 markers. The original header hashes remain in the transport receipts, the raw authorized capture remains outside the repository, and none of the 26 source bodies changed.

The offline verifier binds every archive route to the exact adjudication rank, evidence ordinal, requested URL, source class, support statement, allowed final-host set, body digest, sanitized-header digest, TLS leaf digest, and required source-specific marker. It also binds all four embedded recovery and adjudication inputs byte-for-byte to their current repository paths, validates the capture plan, recomputes the 256-entry raw-capture manifest digest, and cross-checks each retained route, body, and original-header hash against that manifest. Fourteen adversarial mutations test input substitution, plan substitution, capture-manifest substitution, source-body drift, header leakage, host escape, authority widening, and promotion laundering.

## Evidentiary disposition

```text
adjudicated identity candidates                         23
captured source routes                                  26
HTTP 200 / allowed host / TLS verified                  26
captured source-body bytes                        8,754,182
exact brand/domain candidates supported                 19
existing alias/website updates supported                 2
successor-brand continuity candidates supported          2
legal-entity successions established                     0
case receipt admissions                                  0
company registry promotions                              0
company-year promotions                                  0
graph or actor-hop effects                                0
```

This packet establishes custody and bounded semantic support. A later admission transaction must decide which source objects enter the NatSec100 receipt ledger, assign receipt identifiers without rewriting the acquisition facts, and then promote supported company and company-year rows separately. Ranking membership still proves neither procurement nor investment, and source custody does not itself create a case fact, graph edge, or cross-case identity.
