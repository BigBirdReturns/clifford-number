import { mkdirSync, readFileSync, writeFileSync } from 'node:fs';
import { dirname } from 'node:path';
import { compilePreferencePopulationFrameAssuranceFixture, renderPreferencePopulationFrameAssuranceMarkdown } from './lib/preference-population-frame-assurance.mjs';
const fixturePath=process.argv[2]??'data/research/preference-custody/population-frame-assurance.fixture.json';const jsonPath=process.argv[3]??'build/research/preference-population-frame-assurance.json';const markdownPath=process.argv[4]??'build/research/preference-population-frame-assurance.md';const compiled=compilePreferencePopulationFrameAssuranceFixture(JSON.parse(readFileSync(fixturePath,'utf8')));mkdirSync(dirname(jsonPath),{recursive:true});mkdirSync(dirname(markdownPath),{recursive:true});writeFileSync(jsonPath,JSON.stringify(compiled,null,2)+'\n');writeFileSync(markdownPath,renderPreferencePopulationFrameAssuranceMarkdown(compiled));console.log(`compiled ${compiled.fixture_id} -> ${jsonPath}, ${markdownPath}`);
