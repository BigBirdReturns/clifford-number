import { existsSync, readFileSync } from 'node:fs';
import { execFileSync } from 'node:child_process';
import { loadPreferenceCustodyV57SourceBundle, validateBuild, validateManifest } from './lib/preference-custody-manifest-v58.mjs';
const manifestPath=process.argv[2]??'data/research/preference-custody/control-manifest-v58.json'; const buildPath=process.argv[3]??'build/research/preference-custody-laboratory-floor-v58.json'; const load=p=>JSON.parse(readFileSync(p,'utf8'));
let manifest; try {manifest=load(manifestPath);} catch(e) {console.error(`- Preference Custody v58 manifest could not be read: ${e.message}`);process.exit(1);} const manifestErrors=validateManifest(manifest); if(manifestErrors.length){console.error(manifestErrors.map(e=>`- ${e}`).join('\n'));process.exit(1);}
if(!existsSync('build/research/preference-custody-laboratory-floor-v57.json'))execFileSync(process.execPath,['tools/compile-preference-custody-manifest-v57.mjs'],{stdio:'inherit'});if(!existsSync('build/research/preference-linkage-repository-owner-login-normalization-alias-case-location-pair-assurance.json'))execFileSync(process.execPath,['tools/compile-preference-linkage-repository-owner-login-normalization-alias-case-location-pair-assurance.mjs'],{stdio:'inherit'});
let build;try{build=load(buildPath);}catch(e){console.error(`- Preference Custody v58 build could not be read: ${e.message}`);process.exit(1);} const baseBuild=load('build/research/preference-custody-laboratory-floor-v57.json');const targetBuild=load('build/research/preference-linkage-repository-owner-login-normalization-alias-case-location-pair-assurance.json');const targetFixture=load(manifest.extension_control.source_fixture_path);const baseSources=loadPreferenceCustodyV57SourceBundle(load);const errors=validateBuild(build,manifest,baseBuild,targetBuild,targetFixture,baseSources);if(errors.length){console.error(errors.map(e=>`- ${e}`).join('\n'));process.exit(1);}console.log(`validated ${manifest.manifest_id} from ${buildPath}`);
