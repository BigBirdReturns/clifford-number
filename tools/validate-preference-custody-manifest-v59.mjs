import { existsSync, readFileSync } from 'node:fs';
import { execFileSync } from 'node:child_process';
import { loadPreferenceCustodyV58SourceBundle, validateBuild, validateManifest } from './lib/preference-custody-manifest-v59.mjs';
const manifestPath=process.argv[2]??'data/research/preference-custody/control-manifest-v59.json', buildPath=process.argv[3]??'build/research/preference-custody-laboratory-floor-v59.json', load=p=>JSON.parse(readFileSync(p,'utf8')); let manifest; try { manifest=load(manifestPath); } catch(e) { console.error(`- Preference Custody v59 manifest could not be read: ${e.message}`); process.exit(1); } const me=validateManifest(manifest); if (me.length) { console.error(me.map(x=>`- ${x}`).join('\n')); process.exit(1); } if (!existsSync('build/research/preference-custody-laboratory-floor-v58.json')) execFileSync(process.execPath,['test/preference-custody-manifest-v58.test.js'],{stdio:'inherit'}); if (!existsSync('build/research/preference-linkage-repository-owner-login-unicode-casefold-alias-reuse-assurance.json')) execFileSync(process.execPath,['tools/compile-preference-linkage-repository-owner-login-unicode-casefold-alias-reuse-assurance.mjs'],{stdio:'inherit'}); let build; try { build=load(buildPath); } catch(e) { console.error(`- Preference Custody v59 build could not be read: ${e.message}`); process.exit(1); } const baseBuild=load('build/research/preference-custody-laboratory-floor-v58.json'),targetBuild=load('build/research/preference-linkage-repository-owner-login-unicode-casefold-alias-reuse-assurance.json'),targetFixture=load(manifest.extension_control.source_fixture_path),baseSources=loadPreferenceCustodyV58SourceBundle(load); const errors=validateBuild(build,manifest,baseBuild,targetBuild,targetFixture,baseSources); if (errors.length) { console.error(errors.map(x=>`- ${x}`).join('\n')); process.exit(1); } console.log(`validated ${manifest.manifest_id} from ${buildPath}`);
