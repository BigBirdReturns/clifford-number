import { createHash } from 'node:crypto';

export const PREFERENCE_LINKAGE_INTERVAL_METHOD_PARTITION_REPLICATION_DEPLOYMENT_FIXTURE_SCHEMA_VERSION = "preference-linkage-interval-method-partition-replication-deployment-assurance-fixture@1";
export const PREFERENCE_LINKAGE_INTERVAL_METHOD_PARTITION_REPLICATION_DEPLOYMENT_BUILD_SCHEMA_VERSION = "preference-linkage-interval-method-partition-replication-deployment-assurance-build@1";
export const REQUIRED_LINKAGE_INTERVAL_METHOD_PARTITION_REPLICATION_DEPLOYMENT_REFUSAL_RULES = Object.freeze(["one_method_label_is_not_bound_code_configuration_transform_inversion_and_implementation_lineage","one_repository_url_is_not_the_executed_commit_build_artifact_or_entry_point","one_commit_hash_is_not_a_reproducible_build_without_environment_dependency_configuration_and_artifact_custody","one_successful_replay_is_not_deterministic_assurance_across_environments_hardware_and_numerics","one_seed_is_not_stochastic_reproducibility_without_prng_algorithm_state_stream_and_draw_order","one_heldout_file_is_not_independent_when_records_labels_features_derivations_or_model_selection_cross_the_boundary","random_pair_splitting_is_not_leakage_free_when_entities_sources_households_clusters_graph_components_trajectories_or_time_cross_splits","one_partition_identifier_is_not_split_custody_without_generation_grouping_overlap_duplicate_and_derivation_audits","historical_calibration_or_validation_data_are_not_deployment_exchangeability","one_conformal_bootstrap_posterior_or_predictive_result_is_not_valid_after_exchangeability_support_sampling_or_stationarity_failure","one_replication_is_not_independent_when_the_original_team_labels_population_method_choices_or_acceptance_rule_are_reused_or_revised_after_results","aggregate_replication_agreement_is_not_subgroup_source_geography_language_support_tail_or_time_applicability","one_deployment_badge_is_not_applicability_outside_the_frozen_population_policy_workflow_release_and_use","monitoring_without_predeclared_thresholds_safe_decline_rollback_withdrawal_and_corrected_republication_is_not_governance","historical_method_assurance_is_not_current_after_method_code_dependency_configuration_data_partition_label_source_population_workflow_policy_release_deployment_or_use_succession","method_or_applicability_failure_is_not_proof_of_coercion_manipulation_discrimination_breach_misconduct_coordination_common_purpose_or_intent","binding_public_authority_requires_separate_current_public_authorization_receipts"]);
export const LINKAGE_INTERVAL_METHOD_PARTITION_REPLICATION_DEPLOYMENT_FALSE_CLASSIFICATIONS = Object.freeze(["public_method_badge_identifies_executed_code_and_artifact","public_replay_badge_identifies_deterministic_runtime_and_configuration","public_out_of_sample_badge_identifies_leakage_free_partition","public_deployment_badge_identifies_exchangeability_and_scope","one_replication_establishes_independent_applicability","published_coverage_establishes_real_world_effect","graph_effect_present","binding_public_authority_present"]);
export const COMPLETE_LINKAGE_INTERVAL_METHOD_PARTITION_REPLICATION_DEPLOYMENT_CLASSIFICATION = "complete_method_partition_replication_deployment_assurance_supported_in_at_least_one_world";
export const EXPECTED_LINKAGE_INTERVAL_METHOD_PARTITION_REPLICATION_DEPLOYMENT_METRICS = Object.freeze({"worlds":8,"public_method_replay_oos_deployment_signatures":1,"method_governance_signatures":8,"complete_method_assurance_worlds":1,"unbound_method_pairs":100,"configuration_drift_pairs":90,"partition_overlap_pairs":80,"selection_leakage_pairs":70,"exchangeability_violated_pairs":60,"independent_replication_failures":50,"out_of_scope_deployment_decisions":40,"stale_method_decisions":100,"unsupported_deployment_decisions":700,"binding_public_authority_worlds":0});

const FIXTURE_ID = "same-linkage-interval-method-replay-oos-deployment-status-different-assurance-states-v1";
const EXPECTED_BASELINE = Object.freeze({"operative_release":"RELEASE-INCIDENT-V1@1","published_candidate_pairs":100,"published_interval_bearing_pairs":100,"published_nominal_coverage":0.95,"published_empirical_coverage":0.95,"published_interval_misses":5,"published_mean_interval_width":0.02,"public_method_status":"linkage_interval_method_verified","public_reproducibility_status":"deterministic_replay_verified","public_out_of_sample_status":"out_of_sample_validated","public_deployment_status":"deployment_applicability_verified","approved_use":"longitudinal_exposure_estimation"});
const EXPECTED_INTERPRETATION_CONTRACT = Object.freeze({"what_this_is":"A synthetic implementation-custody control separating one complete-looking linkage-interval method, replay, out-of-sample, and deployment surface from eight incompatible code, runtime, partition, assumption, replication, applicability, and lineage states.","what_this_is_not":"A real implementation audit, executable-method verification, leakage finding, exchangeability finding, replication result, deployment-validity claim, causal effect, graph fact, allegation, or public-authority verdict.","copy_ready_caveat":"Verified-looking method, replay, out-of-sample, and deployment badges do not establish the executed code and artifact, frozen configuration and randomness, leakage-free partitions, valid deployment assumptions, independent replication, current applicability, or release succession."});
const EXPECTED_CLASSIFICATION = Object.freeze({"public_method_badge_identifies_executed_code_and_artifact":false,"public_replay_badge_identifies_deterministic_runtime_and_configuration":false,"public_out_of_sample_badge_identifies_leakage_free_partition":false,"public_deployment_badge_identifies_exchangeability_and_scope":false,"one_replication_establishes_independent_applicability":false,"published_coverage_establishes_real_world_effect":false,"graph_effect_present":false,"binding_public_authority_present":false,"complete_method_partition_replication_deployment_assurance_supported_in_at_least_one_world":true});
const EXPECTED_WORLD_IDS = Object.freeze(["complete_method_runtime_partition_replication_deployment_assurance","unbound_or_substituted_method_implementation","configuration_seed_environment_or_numerical_drift","partition_overlap_selection_reuse_or_cross_split_leakage","exchangeability_support_sampling_or_stationarity_failure","nonindependent_or_post_result_replication","out_of_scope_deployment_without_monitoring_or_recovery","stale_inherited_method_and_deployment_assurance"]);
const EXPECTED_WORLD_SNAPSHOT_SHA256 = Object.freeze({"complete_method_runtime_partition_replication_deployment_assurance":"474e13c21b9e57d09178fe21d06e294cb5874d0211922e4c4c4d045cd50a306e","unbound_or_substituted_method_implementation":"5592ec6e364c155da29157264c17a1e4a6cd7ec293cf2ffca6c7abe379a8d7ba","configuration_seed_environment_or_numerical_drift":"e0cca26cabfeba5b2a5c1a05c6f4dd0a13ac4fea2b5b8993c3e52a72eca39a96","partition_overlap_selection_reuse_or_cross_split_leakage":"cde7efedb805d3eda11fa4af282c233dc4ba1b433ce4514c66ef45c47fa701ad","exchangeability_support_sampling_or_stationarity_failure":"23c033e6e3fdeda7695b494ea69d14f55139f185f5271625e5aaf6d6da872059","nonindependent_or_post_result_replication":"5d93e666332ee124d5536013f7da74d279c2bb34a6fc4008a21e8354f1c3646b","out_of_scope_deployment_without_monitoring_or_recovery":"d78f09a8e6d7b319dfd32435ea655bb293273e9fe769535f0c780098a38178d0","stale_inherited_method_and_deployment_assurance":"687e199378e75d3e4b44047ca77662bcdd397e272e5d77e38640439755b3921f"});
const EXPECTED_WORLD_BURDENS = Object.freeze({"complete_method_runtime_partition_replication_deployment_assurance":{"unbound_method_pairs":0,"configuration_drift_pairs":0,"partition_overlap_pairs":0,"selection_leakage_pairs":0,"exchangeability_violated_pairs":0,"independent_replication_failures":0,"out_of_scope_deployment_decisions":0,"stale_method_decisions":0,"unsupported_deployment_decisions":0},"unbound_or_substituted_method_implementation":{"unbound_method_pairs":100,"configuration_drift_pairs":0,"partition_overlap_pairs":0,"selection_leakage_pairs":0,"exchangeability_violated_pairs":0,"independent_replication_failures":0,"out_of_scope_deployment_decisions":0,"stale_method_decisions":0,"unsupported_deployment_decisions":100},"configuration_seed_environment_or_numerical_drift":{"unbound_method_pairs":0,"configuration_drift_pairs":90,"partition_overlap_pairs":0,"selection_leakage_pairs":0,"exchangeability_violated_pairs":0,"independent_replication_failures":0,"out_of_scope_deployment_decisions":0,"stale_method_decisions":0,"unsupported_deployment_decisions":100},"partition_overlap_selection_reuse_or_cross_split_leakage":{"unbound_method_pairs":0,"configuration_drift_pairs":0,"partition_overlap_pairs":80,"selection_leakage_pairs":70,"exchangeability_violated_pairs":0,"independent_replication_failures":0,"out_of_scope_deployment_decisions":0,"stale_method_decisions":0,"unsupported_deployment_decisions":100},"exchangeability_support_sampling_or_stationarity_failure":{"unbound_method_pairs":0,"configuration_drift_pairs":0,"partition_overlap_pairs":0,"selection_leakage_pairs":0,"exchangeability_violated_pairs":60,"independent_replication_failures":0,"out_of_scope_deployment_decisions":0,"stale_method_decisions":0,"unsupported_deployment_decisions":100},"nonindependent_or_post_result_replication":{"unbound_method_pairs":0,"configuration_drift_pairs":0,"partition_overlap_pairs":0,"selection_leakage_pairs":0,"exchangeability_violated_pairs":0,"independent_replication_failures":50,"out_of_scope_deployment_decisions":0,"stale_method_decisions":0,"unsupported_deployment_decisions":100},"out_of_scope_deployment_without_monitoring_or_recovery":{"unbound_method_pairs":0,"configuration_drift_pairs":0,"partition_overlap_pairs":0,"selection_leakage_pairs":0,"exchangeability_violated_pairs":0,"independent_replication_failures":0,"out_of_scope_deployment_decisions":40,"stale_method_decisions":0,"unsupported_deployment_decisions":100},"stale_inherited_method_and_deployment_assurance":{"unbound_method_pairs":0,"configuration_drift_pairs":0,"partition_overlap_pairs":0,"selection_leakage_pairs":0,"exchangeability_violated_pairs":0,"independent_replication_failures":0,"out_of_scope_deployment_decisions":0,"stale_method_decisions":100,"unsupported_deployment_decisions":100}});
const EXPECTED_MECHANISMS = Object.freeze({"complete_method_runtime_partition_replication_deployment_assurance":"complete_method_runtime_partition_replication_deployment_assurance","unbound_or_substituted_method_implementation":"unbound_or_substituted_method_implementation","configuration_seed_environment_or_numerical_drift":"configuration_seed_environment_or_numerical_drift","partition_overlap_selection_reuse_or_cross_split_leakage":"partition_overlap_selection_reuse_or_cross_split_leakage","exchangeability_support_sampling_or_stationarity_failure":"exchangeability_support_sampling_or_stationarity_failure","nonindependent_or_post_result_replication":"nonindependent_or_post_result_replication","out_of_scope_deployment_without_monitoring_or_recovery":"out_of_scope_deployment_without_monitoring_or_recovery","stale_inherited_method_and_deployment_assurance":"stale_inherited_method_and_deployment_assurance"});
const EXPECTED_DESCRIPTIONS = Object.freeze({"complete_method_runtime_partition_replication_deployment_assurance":"Executed code, build artifact, runtime, configuration, randomness, partitions, assumptions, independent replication, deployment scope, monitoring, correction, and current lineage are complete.","unbound_or_substituted_method_implementation":"The public method badge is unchanged although the repository, commit, reviewed source, build artifact, entry point, transform, inversion rule, or executed implementation is unbound or substituted.","configuration_seed_environment_or_numerical_drift":"The public replay badge is unchanged although configuration, seed, PRNG state, environment, dependency, compiler, hardware, precision, deterministic-operation, or replay custody has drifted.","partition_overlap_selection_reuse_or_cross_split_leakage":"The public out-of-sample badge is unchanged although construction, calibration, validation, holdout, monitoring, labels, entities, sources, clusters, graph components, trajectories, or time cross partitions or model selection reuses held-out information.","exchangeability_support_sampling_or_stationarity_failure":"The public deployment badge is unchanged although exchangeability, positivity, support, sampling, stationarity, missingness, shift testing, or safe-decline assumptions fail.","nonindependent_or_post_result_replication":"The public reproducibility badge is unchanged although the replication reuses the original team, labels, population, method choices, acceptance rule, or adapts after seeing results.","out_of_scope_deployment_without_monitoring_or_recovery":"The public deployment-applicability badge is unchanged although use leaves the validated population, subgroup, support, policy, workflow, release, or purpose and lacks monitoring, triggers, abstention, rollback, withdrawal, corrected republication, or appeal.","stale_inherited_method_and_deployment_assurance":"Historical assurance is inherited after method, code, dependency, configuration, data, partition, label, source, population, workflow, policy, release, deployment, or use succession without complete requalification."});
const BOOLEAN_KEYS = Object.freeze({"implementation":["method_specification_frozen","repository_bound","commit_bound","source_tree_bound","reviewed_files_bound","build_artifact_bound","executable_entry_point_bound","transform_bound","inversion_rule_bound","artifact_digest_verified","executed_code_matches_reviewed_code"],"runtime_configuration":["configuration_frozen","parameter_file_bound","random_seed_bound","prng_algorithm_state_stream_draw_order_bound","environment_image_bound","dependency_lock_bound","compiler_runtime_bound","hardware_bound","numerical_precision_bound","deterministic_operations_policy_bound","replay_receipt_verified","replay_cross_environment_consistent"],"partition":["split_generation_algorithm_bound","training_tuning_construction_calibration_validation_holdout_monitoring_label_partitions_disjoint","entity_source_household_cluster_graph_trajectory_time_groups_disjoint","duplicate_and_derivation_audit_complete","cross_split_leakage_absent","model_selection_reuse_absent"],"exchangeability":["exchangeability_supported","positivity_supported","support_overlap_supported","sampling_assumptions_supported","stationarity_supported","missingness_supported","deployment_shift_tests_current","safe_decline_on_assumption_failure"],"replication":["independent_team","independent_labels","independent_population","method_and_configuration_precommitted","acceptance_rule_precommitted","blinded_until_freeze","no_post_result_adaptation","discordance_disposition_defined"],"deployment":["validated_population_match","validated_subgroup_match","validated_support_and_tail_match","validated_policy_workflow_release_use_match","active_shift_monitoring","predeclared_triggers","abstention_available","rollback_available","certificate_withdrawal_available","corrected_republication_available","appeal_available"],"governance":["assurance_current","succession_ledger_complete","correction_and_retirement_defined","binding_public_authority"]});
const BURDEN_LOCATIONS = Object.freeze({"unbound_method_pairs":["implementation","unbound_method_pairs"],"configuration_drift_pairs":["runtime_configuration","configuration_drift_pairs"],"partition_overlap_pairs":["partition","partition_overlap_pairs"],"selection_leakage_pairs":["partition","selection_leakage_pairs"],"exchangeability_violated_pairs":["exchangeability","exchangeability_violated_pairs"],"independent_replication_failures":["replication","independent_replication_failures"],"out_of_scope_deployment_decisions":["deployment","out_of_scope_deployment_decisions"],"stale_method_decisions":["governance","stale_method_decisions"],"unsupported_deployment_decisions":["governance","unsupported_deployment_decisions"]});
const FLAG_KEYS = Object.freeze(["complete_implementation_custody","complete_runtime_configuration_custody","complete_partition_custody","complete_exchangeability_assurance","complete_independent_replication_assurance","complete_deployment_applicability_assurance","current_method_lineage_assurance","complete_method_partition_replication_deployment_assurance"]);
const EXPECTED_FIXTURE_KEYS = Object.freeze(["schema_version","fixture_id","issue","parent_program_issue","captured_at","status","graph_effect","counts_toward_thesis_evidence","baseline","interpretation_contract","required_refusal_rules","expected_classification","worlds"]);
const EXPECTED_BASELINE_KEYS = Object.freeze(["operative_release","published_candidate_pairs","published_interval_bearing_pairs","published_nominal_coverage","published_empirical_coverage","published_interval_misses","published_mean_interval_width","public_method_status","public_reproducibility_status","public_out_of_sample_status","public_deployment_status","approved_use"]);
const EXPECTED_INTERPRETATION_KEYS = Object.freeze(["what_this_is","what_this_is_not","copy_ready_caveat"]);
const EXPECTED_CLASSIFICATION_KEYS = Object.freeze(["public_method_badge_identifies_executed_code_and_artifact","public_replay_badge_identifies_deterministic_runtime_and_configuration","public_out_of_sample_badge_identifies_leakage_free_partition","public_deployment_badge_identifies_exchangeability_and_scope","one_replication_establishes_independent_applicability","published_coverage_establishes_real_world_effect","graph_effect_present","binding_public_authority_present","complete_method_partition_replication_deployment_assurance_supported_in_at_least_one_world"]);
const EXPECTED_WORLD_KEYS = Object.freeze(["world_id","description","implementation","runtime_configuration","partition","exchangeability","replication","deployment","governance","expected_mechanism","expected_flags"]);
const EXPECTED_SECTION_KEYS = Object.freeze({"implementation":["method_specification_frozen","repository_bound","commit_bound","source_tree_bound","reviewed_files_bound","build_artifact_bound","executable_entry_point_bound","transform_bound","inversion_rule_bound","artifact_digest_verified","executed_code_matches_reviewed_code","unbound_method_pairs"],"runtime_configuration":["configuration_frozen","parameter_file_bound","random_seed_bound","prng_algorithm_state_stream_draw_order_bound","environment_image_bound","dependency_lock_bound","compiler_runtime_bound","hardware_bound","numerical_precision_bound","deterministic_operations_policy_bound","replay_receipt_verified","replay_cross_environment_consistent","configuration_drift_pairs"],"partition":["split_generation_algorithm_bound","training_tuning_construction_calibration_validation_holdout_monitoring_label_partitions_disjoint","entity_source_household_cluster_graph_trajectory_time_groups_disjoint","duplicate_and_derivation_audit_complete","cross_split_leakage_absent","model_selection_reuse_absent","partition_overlap_pairs","selection_leakage_pairs"],"exchangeability":["exchangeability_supported","positivity_supported","support_overlap_supported","sampling_assumptions_supported","stationarity_supported","missingness_supported","deployment_shift_tests_current","safe_decline_on_assumption_failure","exchangeability_violated_pairs"],"replication":["independent_team","independent_labels","independent_population","method_and_configuration_precommitted","acceptance_rule_precommitted","blinded_until_freeze","no_post_result_adaptation","discordance_disposition_defined","independent_replication_failures"],"deployment":["validated_population_match","validated_subgroup_match","validated_support_and_tail_match","validated_policy_workflow_release_use_match","active_shift_monitoring","predeclared_triggers","abstention_available","rollback_available","certificate_withdrawal_available","corrected_republication_available","appeal_available","out_of_scope_deployment_decisions"],"governance":["assurance_current","succession_ledger_complete","correction_and_retirement_defined","binding_public_authority","stale_method_decisions","unsupported_deployment_decisions"]});
const BURDEN_KEYS = Object.freeze(Object.keys(BURDEN_LOCATIONS));
const EXPECTED_COMPILED_WORLD_KEYS = Object.freeze([
  'world_id','description','public_surface','public_signature_sha256',
  'implementation','runtime_configuration','partition','exchangeability','replication','deployment','governance',
  'expected_mechanism','expected_flags','observed_flags','burdens','method_governance_signature_sha256'
]);
const EXPECTED_BUILD_KEYS = Object.freeze([
  'schema_version','fixture_id','issue','parent_program_issue','captured_at','status','graph_effect',
  'counts_toward_thesis_evidence','conclusion_generated','real_world_evidence_state','source_fixture_sha256',
  'required_refusal_rules','baseline','worlds','metrics','classification','custody_chain','custody_chain_head_sha256',
  'interpretation_contract'
]);
const EXPECTED_CUSTODY_EVENT_KEYS = Object.freeze([
  'authority','event_id','event_sha256','event_type','evidence_class','payload','previous_event_sha256','source_event_ids'
]);

const object = value => value && typeof value === 'object' && !Array.isArray(value) ? value : {};
const array = value => Array.isArray(value) ? value : [];
const text = value => String(value ?? '').trim();
const unique = values => [...new Set(array(values).map(value => String(value)).filter(Boolean))];
const sorted = values => [...values].sort((left, right) => String(left).localeCompare(String(right)));
const canonical = value => Array.isArray(value)
  ? value.map(canonical)
  : value && typeof value === 'object'
    ? Object.fromEntries(Object.keys(value).sort().map(key => [key, canonical(value[key])]))
    : value;
const stable = value => JSON.stringify(canonical(value));
const sha256 = value => createHash('sha256').update(stable(value)).digest('hex');
const pick = (value, keys) => Object.fromEntries(keys.map(key => [key, value?.[key]]));
const isoDate = value => {
  if (typeof value !== 'string' || !/^\d{4}-\d{2}-\d{2}$/.test(value)) return false;
  const parsed = new Date(`${value}T00:00:00Z`);
  return Number.isFinite(parsed.getTime()) && parsed.toISOString().slice(0, 10) === value;
};
const nonNegativeInteger = value => Number.isInteger(value) && value >= 0;
const requireFalse = (value, label, errors) => { if (value !== false) errors.push(`${label} must remain false`); };
function requireExactKeys(value, expected, label, errors) {
  const keys = Object.keys(object(value));
  if (stable(sorted(keys)) !== stable(sorted(expected)) || keys.length !== expected.length) errors.push(`${label} key ledger mismatch`);
}
function requireBooleanKeys(value, keys, label, errors) {
  for (const key of keys) if (typeof value?.[key] !== 'boolean') errors.push(`${label}.${key} must be boolean`);
}
function seal(event, previousEventSha256) {
  const unsigned = { ...canonical(event), previous_event_sha256: previousEventSha256 };
  return { ...unsigned, event_sha256: sha256(unsigned) };
}

function projectWorld(world) {
  return {
    world_id: world?.world_id,
    description: world?.description,
    implementation: pick(world?.implementation, EXPECTED_SECTION_KEYS.implementation),
    runtime_configuration: pick(world?.runtime_configuration, EXPECTED_SECTION_KEYS.runtime_configuration),
    partition: pick(world?.partition, EXPECTED_SECTION_KEYS.partition),
    exchangeability: pick(world?.exchangeability, EXPECTED_SECTION_KEYS.exchangeability),
    replication: pick(world?.replication, EXPECTED_SECTION_KEYS.replication),
    deployment: pick(world?.deployment, EXPECTED_SECTION_KEYS.deployment),
    governance: pick(world?.governance, EXPECTED_SECTION_KEYS.governance),
    expected_mechanism: world?.expected_mechanism,
    expected_flags: pick(world?.expected_flags, FLAG_KEYS)
  };
}

function projectFixture(fixture) {
  return {
    schema_version: fixture?.schema_version,
    fixture_id: fixture?.fixture_id,
    issue: fixture?.issue,
    parent_program_issue: fixture?.parent_program_issue,
    captured_at: fixture?.captured_at,
    status: fixture?.status,
    graph_effect: fixture?.graph_effect,
    counts_toward_thesis_evidence: fixture?.counts_toward_thesis_evidence,
    baseline: pick(fixture?.baseline, EXPECTED_BASELINE_KEYS),
    interpretation_contract: pick(fixture?.interpretation_contract, EXPECTED_INTERPRETATION_KEYS),
    required_refusal_rules: [...array(fixture?.required_refusal_rules)],
    expected_classification: pick(fixture?.expected_classification, EXPECTED_CLASSIFICATION_KEYS),
    worlds: array(fixture?.worlds).map(projectWorld)
  };
}

function fixtureSnapshot(fixture) { return sha256(projectFixture(fixture)); }

function numericBurden(world) {
  return Object.fromEntries(BURDEN_KEYS.map(key => {
    const [section, field] = BURDEN_LOCATIONS[key];
    return [key, world?.[section]?.[field]];
  }));
}

function classifyWorld(world) {
  const sectionComplete = (section, burdenKeys) => BOOLEAN_KEYS[section].every(key => world?.[section]?.[key] === true)
    && burdenKeys.every(key => {
      const [sourceSection, field] = BURDEN_LOCATIONS[key];
      return world?.[sourceSection]?.[field] === 0;
    });
  const completeImplementation = sectionComplete('implementation', ['unbound_method_pairs']);
  const completeRuntime = sectionComplete('runtime_configuration', ['configuration_drift_pairs']);
  const completePartition = sectionComplete('partition', ['partition_overlap_pairs','selection_leakage_pairs']);
  const completeExchangeability = sectionComplete('exchangeability', ['exchangeability_violated_pairs']);
  const completeReplication = sectionComplete('replication', ['independent_replication_failures']);
  const completeDeployment = sectionComplete('deployment', ['out_of_scope_deployment_decisions']);
  const currentLineage = world?.governance?.assurance_current === true
    && world?.governance?.succession_ledger_complete === true
    && world?.governance?.correction_and_retirement_defined === true
    && world?.governance?.binding_public_authority === false
    && world?.governance?.stale_method_decisions === 0
    && world?.governance?.unsupported_deployment_decisions === 0;
  return {
    complete_implementation_custody: completeImplementation,
    complete_runtime_configuration_custody: completeRuntime,
    complete_partition_custody: completePartition,
    complete_exchangeability_assurance: completeExchangeability,
    complete_independent_replication_assurance: completeReplication,
    complete_deployment_applicability_assurance: completeDeployment,
    current_method_lineage_assurance: currentLineage,
    complete_method_partition_replication_deployment_assurance: completeImplementation && completeRuntime && completePartition && completeExchangeability && completeReplication && completeDeployment && currentLineage
  };
}

function governancePayload(world) {
  return {
    implementation: pick(world?.implementation, EXPECTED_SECTION_KEYS.implementation),
    runtime_configuration: pick(world?.runtime_configuration, EXPECTED_SECTION_KEYS.runtime_configuration),
    partition: pick(world?.partition, EXPECTED_SECTION_KEYS.partition),
    exchangeability: pick(world?.exchangeability, EXPECTED_SECTION_KEYS.exchangeability),
    replication: pick(world?.replication, EXPECTED_SECTION_KEYS.replication),
    deployment: pick(world?.deployment, EXPECTED_SECTION_KEYS.deployment),
    governance: pick(world?.governance, EXPECTED_SECTION_KEYS.governance),
    expected_mechanism: world?.expected_mechanism,
    observed_flags: pick(world?.observed_flags ?? classifyWorld(world), FLAG_KEYS),
    burdens: pick(world?.burdens ?? numericBurden(world), BURDEN_KEYS)
  };
}

function computeMetrics(worlds) {
  const publicSignatures = unique(worlds.map(() => sha256(EXPECTED_BASELINE))).length;
  const governanceSignatures = unique(worlds.map(world => sha256(governancePayload(world)))).length;
  const metrics = {
    worlds: worlds.length,
    public_method_replay_oos_deployment_signatures: publicSignatures,
    method_governance_signatures: governanceSignatures,
    complete_method_assurance_worlds: worlds.filter(world => classifyWorld(world).complete_method_partition_replication_deployment_assurance).length,
    ...Object.fromEntries(BURDEN_KEYS.map(key => [key, worlds.reduce((sum, world) => sum + Number(numericBurden(world)[key] ?? 0), 0)])),
    binding_public_authority_worlds: worlds.filter(world => world?.governance?.binding_public_authority === true).length
  };
  return metrics;
}

function custodyChain(fixture, worlds, metrics, classification) {
  const events = [];
  let previous = null;
  const push = event => {
    const sealed = seal(event, previous);
    events.push(sealed);
    previous = sealed.event_sha256;
  };
  push({
    event_id: `${fixture.fixture_id}:public-surface`,
    event_type: 'linkage_interval_method_replay_oos_deployment_surface_frozen',
    evidence_class: 'synthetic_public_surface',
    authority: 'pc48_fixture_compiler',
    source_event_ids: [],
    payload: { public_surface: canonical(EXPECTED_BASELINE), public_signature_sha256: sha256(EXPECTED_BASELINE) }
  });
  push({
    event_id: `${fixture.fixture_id}:worlds`,
    event_type: 'method_runtime_partition_replication_deployment_worlds_frozen',
    evidence_class: 'synthetic_governance_states',
    authority: 'pc48_fixture_compiler',
    source_event_ids: [`${fixture.fixture_id}:public-surface`],
    payload: { world_ids: worlds.map(world => world.world_id), world_snapshot_sha256: canonical(EXPECTED_WORLD_SNAPSHOT_SHA256) }
  });
  push({
    event_id: `${fixture.fixture_id}:classification`,
    event_type: 'method_assurance_classification_sealed',
    evidence_class: 'synthetic_control_classification',
    authority: 'pc48_fixture_compiler',
    source_event_ids: [`${fixture.fixture_id}:worlds`],
    payload: { classification }
  });
  push({
    event_id: `${fixture.fixture_id}:burdens`,
    event_type: 'bounded_method_and_deployment_burdens_sealed',
    evidence_class: 'synthetic_bounded_metrics',
    authority: 'pc48_fixture_compiler',
    source_event_ids: [`${fixture.fixture_id}:classification`],
    payload: { metrics }
  });
  push({
    event_id: `${fixture.fixture_id}:refusals`,
    event_type: 'method_and_deployment_refusal_ledger_sealed',
    evidence_class: 'laboratory_refusal_contract',
    authority: 'pc48_fixture_compiler',
    source_event_ids: [`${fixture.fixture_id}:burdens`],
    payload: { required_refusal_rules: [...REQUIRED_LINKAGE_INTERVAL_METHOD_PARTITION_REPLICATION_DEPLOYMENT_REFUSAL_RULES] }
  });
  push({
    event_id: `${fixture.fixture_id}:interpretation`,
    event_type: 'interpretation_sealed',
    evidence_class: 'candidate_inference',
    authority: 'pc48_analyst',
    source_event_ids: [`${fixture.fixture_id}:refusals`],
    payload: { interpretation_contract: canonical(EXPECTED_INTERPRETATION_CONTRACT), graph_effect: 'none', real_world_evidence_state: 'none' }
  });
  return events;
}

export function validatePreferenceLinkageIntervalMethodPartitionReplicationDeploymentFixture(fixture) {
  const errors = [];
  requireExactKeys(fixture, EXPECTED_FIXTURE_KEYS, 'PC-48 fixture', errors);
  if (fixture?.schema_version !== PREFERENCE_LINKAGE_INTERVAL_METHOD_PARTITION_REPLICATION_DEPLOYMENT_FIXTURE_SCHEMA_VERSION) errors.push('PC-48 fixture schema mismatch');
  if (fixture?.fixture_id !== FIXTURE_ID) errors.push('PC-48 fixture identity mismatch');
  if (fixture?.issue !== 1064 || fixture?.parent_program_issue !== 594) errors.push('PC-48 fixture issue custody mismatch');
  if (!isoDate(fixture?.captured_at)) errors.push('PC-48 fixture captured_at must be an exact ISO date');
  if (fixture?.status !== 'synthetic_control' || fixture?.graph_effect !== 'none') errors.push('PC-48 fixture status or graph effect mismatch');
  requireFalse(fixture?.counts_toward_thesis_evidence, 'PC-48 fixture thesis evidence', errors);
  requireExactKeys(fixture?.baseline, EXPECTED_BASELINE_KEYS, 'PC-48 fixture baseline', errors);
  if (stable(fixture?.baseline) !== stable(EXPECTED_BASELINE)) errors.push('PC-48 fixture frozen public surface mismatch');
  requireExactKeys(fixture?.interpretation_contract, EXPECTED_INTERPRETATION_KEYS, 'PC-48 fixture interpretation contract', errors);
  if (stable(fixture?.interpretation_contract) !== stable(EXPECTED_INTERPRETATION_CONTRACT)) errors.push('PC-48 fixture interpretation contract mismatch');
  if (stable(fixture?.required_refusal_rules) !== stable(REQUIRED_LINKAGE_INTERVAL_METHOD_PARTITION_REPLICATION_DEPLOYMENT_REFUSAL_RULES)) errors.push('PC-48 fixture refusal-rule ledger mismatch');
  requireExactKeys(fixture?.expected_classification, EXPECTED_CLASSIFICATION_KEYS, 'PC-48 fixture classification', errors);
  if (stable(fixture?.expected_classification) !== stable(EXPECTED_CLASSIFICATION)) errors.push('PC-48 fixture classification mismatch');

  const worlds = array(fixture?.worlds);
  if (worlds.length !== 8 || stable(worlds.map(world => world?.world_id)) !== stable(EXPECTED_WORLD_IDS)) errors.push('PC-48 fixture must contain the eight exact ordered worlds');
  for (const world of worlds) {
    const id = text(world?.world_id);
    requireExactKeys(world, EXPECTED_WORLD_KEYS, `PC-48 fixture world ${id}`, errors);
    for (const section of Object.keys(EXPECTED_SECTION_KEYS)) {
      requireExactKeys(world?.[section], EXPECTED_SECTION_KEYS[section], `PC-48 fixture world ${id} ${section}`, errors);
      requireBooleanKeys(world?.[section], BOOLEAN_KEYS[section], `PC-48 fixture world ${id} ${section}`, errors);
    }
    requireExactKeys(world?.expected_flags, FLAG_KEYS, `PC-48 fixture world ${id} expected_flags`, errors);
    requireBooleanKeys(world?.expected_flags, FLAG_KEYS, `PC-48 fixture world ${id} expected_flags`, errors);
    if (!text(world?.description)) errors.push(`PC-48 fixture world ${id} description is required`);
    if (world?.description !== EXPECTED_DESCRIPTIONS[id]) errors.push(`PC-48 fixture world ${id} description mismatch`);
    if (world?.expected_mechanism !== EXPECTED_MECHANISMS[id]) errors.push(`PC-48 fixture world ${id} mechanism mismatch`);
    const flags = classifyWorld(world);
    if (stable(world?.expected_flags) !== stable(flags)) errors.push(`PC-48 fixture world ${id} flag mismatch`);
    const burdens = numericBurden(world);
    for (const [key, value] of Object.entries(burdens)) if (!nonNegativeInteger(value)) errors.push(`PC-48 fixture world ${id} burden ${key} must be a nonnegative integer`);
    if (!EXPECTED_WORLD_BURDENS[id] || stable(burdens) !== stable(EXPECTED_WORLD_BURDENS[id])) errors.push(`PC-48 fixture world ${id} burden-state mismatch`);
    if (sha256(projectWorld(world)) !== EXPECTED_WORLD_SNAPSHOT_SHA256[id]) errors.push(`PC-48 fixture world ${id} snapshot mismatch`);
    if (world?.governance?.binding_public_authority !== false) errors.push(`PC-48 fixture world ${id} binding authority must remain false`);
  }
  const metrics = computeMetrics(worlds);
  if (stable(metrics) !== stable(EXPECTED_LINKAGE_INTERVAL_METHOD_PARTITION_REPLICATION_DEPLOYMENT_METRICS)) errors.push('PC-48 fixture metric ledger mismatch');
  return errors;
}

export function compilePreferenceLinkageIntervalMethodPartitionReplicationDeploymentFixture(fixture) {
  const sourceErrors = validatePreferenceLinkageIntervalMethodPartitionReplicationDeploymentFixture(fixture);
  if (sourceErrors.length) throw new Error(sourceErrors.join('; '));
  const compiledWorlds = fixture.worlds.map(world => {
    const projected = projectWorld(world);
    const burdens = numericBurden(world);
    const observedFlags = classifyWorld(world);
    const compiledWorld = {
      world_id: projected.world_id,
      description: projected.description,
      public_surface: canonical(EXPECTED_BASELINE),
      public_signature_sha256: sha256(EXPECTED_BASELINE),
      implementation: projected.implementation,
      runtime_configuration: projected.runtime_configuration,
      partition: projected.partition,
      exchangeability: projected.exchangeability,
      replication: projected.replication,
      deployment: projected.deployment,
      governance: projected.governance,
      expected_mechanism: projected.expected_mechanism,
      expected_flags: projected.expected_flags,
      observed_flags: observedFlags,
      burdens,
      method_governance_signature_sha256: null
    };
    compiledWorld.method_governance_signature_sha256 = sha256(governancePayload(compiledWorld));
    return compiledWorld;
  });
  const metrics = {
    worlds: compiledWorlds.length,
    public_method_replay_oos_deployment_signatures: unique(compiledWorlds.map(world => world.public_signature_sha256)).length,
    method_governance_signatures: unique(compiledWorlds.map(world => world.method_governance_signature_sha256)).length,
    complete_method_assurance_worlds: compiledWorlds.filter(world => world.observed_flags.complete_method_partition_replication_deployment_assurance).length,
    ...Object.fromEntries(BURDEN_KEYS.map(key => [key, compiledWorlds.reduce((sum, world) => sum + world.burdens[key], 0)])),
    binding_public_authority_worlds: compiledWorlds.filter(world => world.governance.binding_public_authority === true).length
  };
  const classification = canonical(EXPECTED_CLASSIFICATION);
  const custody = custodyChain(fixture, compiledWorlds, metrics, classification);
  return {
    schema_version: PREFERENCE_LINKAGE_INTERVAL_METHOD_PARTITION_REPLICATION_DEPLOYMENT_BUILD_SCHEMA_VERSION,
    fixture_id: fixture.fixture_id,
    issue: fixture.issue,
    parent_program_issue: fixture.parent_program_issue,
    captured_at: fixture.captured_at,
    status: 'synthetic_linkage_interval_method_partition_replication_deployment_control_compiled',
    graph_effect: 'none',
    counts_toward_thesis_evidence: false,
    conclusion_generated: false,
    real_world_evidence_state: 'none',
    source_fixture_sha256: fixtureSnapshot(fixture),
    required_refusal_rules: [...REQUIRED_LINKAGE_INTERVAL_METHOD_PARTITION_REPLICATION_DEPLOYMENT_REFUSAL_RULES],
    baseline: canonical(EXPECTED_BASELINE),
    worlds: compiledWorlds,
    metrics,
    classification,
    custody_chain: custody,
    custody_chain_head_sha256: custody.at(-1).event_sha256,
    interpretation_contract: canonical(EXPECTED_INTERPRETATION_CONTRACT)
  };
}

function validateChain(compiled, errors) {
  const events = array(compiled?.custody_chain);
  if (events.length !== 6) errors.push('compiled PC-48 custody chain must contain six events');
  let previous = null;
  const seen = new Set();
  for (const event of events) {
    requireExactKeys(event, EXPECTED_CUSTODY_EVENT_KEYS, 'compiled PC-48 custody event', errors);
    if (event?.previous_event_sha256 !== previous) errors.push('compiled PC-48 custody previous hash mismatch');
    for (const sourceId of array(event?.source_event_ids)) if (!seen.has(sourceId)) errors.push('compiled PC-48 custody source missing');
    const unsigned = { ...event };
    delete unsigned.event_sha256;
    if (event?.event_sha256 !== sha256(unsigned)) errors.push('compiled PC-48 custody event hash mismatch');
    if (text(event?.event_id)) seen.add(event.event_id);
    previous = event?.event_sha256;
  }
  if (previous !== compiled?.custody_chain_head_sha256) errors.push('compiled PC-48 custody head mismatch');
}

export function validatePreferenceLinkageIntervalMethodPartitionReplicationDeploymentBuild(compiled, fixture) {
  const errors = [];
  requireExactKeys(compiled, EXPECTED_BUILD_KEYS, 'compiled PC-48', errors);
  if (compiled?.schema_version !== PREFERENCE_LINKAGE_INTERVAL_METHOD_PARTITION_REPLICATION_DEPLOYMENT_BUILD_SCHEMA_VERSION) errors.push('compiled PC-48 schema mismatch');
  if (compiled?.fixture_id !== FIXTURE_ID || compiled?.issue !== 1064 || compiled?.parent_program_issue !== 594) errors.push('compiled PC-48 identity mismatch');
  if (!isoDate(compiled?.captured_at)) errors.push('compiled PC-48 captured_at must be an exact ISO date');
  if (fixture && compiled?.captured_at !== fixture?.captured_at) errors.push('compiled PC-48 captured_at must equal fixture captured_at');
  if (compiled?.status !== 'synthetic_linkage_interval_method_partition_replication_deployment_control_compiled' || compiled?.graph_effect !== 'none' || compiled?.real_world_evidence_state !== 'none') errors.push('compiled PC-48 status boundary mismatch');
  requireFalse(compiled?.counts_toward_thesis_evidence, 'compiled PC-48 thesis evidence', errors);
  requireFalse(compiled?.conclusion_generated, 'compiled PC-48 conclusion', errors);
  if (!fixture) errors.push('compiled PC-48 fixture source is required');
  if (!/^[0-9a-f]{64}$/.test(text(compiled?.source_fixture_sha256))) errors.push('compiled PC-48 source hash invalid');
  if (fixture && compiled?.source_fixture_sha256 !== fixtureSnapshot(fixture)) errors.push('compiled PC-48 source fixture hash mismatch');
  requireExactKeys(compiled?.baseline, EXPECTED_BASELINE_KEYS, 'compiled PC-48 baseline', errors);
  if (stable(compiled?.baseline) !== stable(EXPECTED_BASELINE)) errors.push('compiled PC-48 baseline mismatch');
  requireExactKeys(compiled?.interpretation_contract, EXPECTED_INTERPRETATION_KEYS, 'compiled PC-48 interpretation contract', errors);
  if (stable(compiled?.interpretation_contract) !== stable(EXPECTED_INTERPRETATION_CONTRACT)) errors.push('compiled PC-48 interpretation contract mismatch');
  if (stable(compiled?.required_refusal_rules) !== stable(REQUIRED_LINKAGE_INTERVAL_METHOD_PARTITION_REPLICATION_DEPLOYMENT_REFUSAL_RULES)) errors.push('compiled PC-48 refusal ledger mismatch');
  requireExactKeys(compiled?.metrics, Object.keys(EXPECTED_LINKAGE_INTERVAL_METHOD_PARTITION_REPLICATION_DEPLOYMENT_METRICS), 'compiled PC-48 metrics', errors);
  requireExactKeys(compiled?.classification, EXPECTED_CLASSIFICATION_KEYS, 'compiled PC-48 classification', errors);
  if (stable(compiled?.classification) !== stable(EXPECTED_CLASSIFICATION)) errors.push('compiled PC-48 classification mismatch');
  const worlds = array(compiled?.worlds);
  if (worlds.length !== 8 || stable(worlds.map(world => world?.world_id)) !== stable(EXPECTED_WORLD_IDS)) errors.push('compiled PC-48 world ledger mismatch');
  if (unique(worlds.map(world => world?.public_signature_sha256)).length !== 1) errors.push('compiled PC-48 must preserve one public signature');
  if (unique(worlds.map(world => world?.method_governance_signature_sha256)).length !== 8) errors.push('compiled PC-48 must preserve eight method-governance signatures');
  for (const world of worlds) {
    const id = text(world?.world_id);
    requireExactKeys(world, EXPECTED_COMPILED_WORLD_KEYS, `compiled PC-48 world ${id}`, errors);
    for (const section of Object.keys(EXPECTED_SECTION_KEYS)) {
      requireExactKeys(world?.[section], EXPECTED_SECTION_KEYS[section], `compiled PC-48 world ${id} ${section}`, errors);
      requireBooleanKeys(world?.[section], BOOLEAN_KEYS[section], `compiled PC-48 world ${id} ${section}`, errors);
    }
    requireExactKeys(world?.expected_flags, FLAG_KEYS, `compiled PC-48 world ${id} expected_flags`, errors);
    requireExactKeys(world?.observed_flags, FLAG_KEYS, `compiled PC-48 world ${id} observed_flags`, errors);
    requireExactKeys(world?.burdens, BURDEN_KEYS, `compiled PC-48 world ${id} burdens`, errors);
    requireExactKeys(world?.public_surface, EXPECTED_BASELINE_KEYS, `compiled PC-48 world ${id} public_surface`, errors);
    if (stable(world?.public_surface) !== stable(EXPECTED_BASELINE)) errors.push(`compiled PC-48 world ${id} public surface mismatch`);
    if (world?.public_signature_sha256 !== sha256(EXPECTED_BASELINE)) errors.push(`compiled PC-48 world ${id} public signature mismatch`);
    if (world?.method_governance_signature_sha256 !== sha256(governancePayload(world))) errors.push(`compiled PC-48 world ${id} method-governance signature mismatch`);
    if (world?.description !== EXPECTED_DESCRIPTIONS[id]) errors.push(`compiled PC-48 world ${id} description mismatch`);
    if (world?.expected_mechanism !== EXPECTED_MECHANISMS[id]) errors.push(`compiled PC-48 world ${id} mechanism mismatch`);
    if (sha256(projectWorld(world)) !== EXPECTED_WORLD_SNAPSHOT_SHA256[id]) errors.push(`compiled PC-48 world ${id} snapshot mismatch`);
    const flags = classifyWorld(world);
    if (stable(world?.expected_flags) !== stable(flags) || stable(world?.observed_flags) !== stable(flags)) errors.push(`compiled PC-48 world ${id} flag mismatch`);
    const burdens = numericBurden(world);
    if (stable(world?.burdens) !== stable(burdens)) errors.push(`compiled PC-48 world ${id} burden copy mismatch`);
    if (!EXPECTED_WORLD_BURDENS[id] || stable(burdens) !== stable(EXPECTED_WORLD_BURDENS[id])) errors.push(`compiled PC-48 world ${id} burden-state mismatch`);
    if (world?.governance?.binding_public_authority !== false) errors.push(`compiled PC-48 world ${id} binding authority must remain false`);
  }
  for (const [key, expected] of Object.entries(EXPECTED_LINKAGE_INTERVAL_METHOD_PARTITION_REPLICATION_DEPLOYMENT_METRICS)) if (compiled?.metrics?.[key] !== expected) errors.push(`compiled PC-48 metric mismatch: ${key}`);
  validateChain(compiled, errors);
  if (fixture && !errors.length) {
    const fixtureErrors = validatePreferenceLinkageIntervalMethodPartitionReplicationDeploymentFixture(fixture);
    if (fixtureErrors.length) errors.push(...fixtureErrors.map(error => `compiled PC-48 fixture invalid: ${error}`));
    else {
      try {
        const expected = compilePreferenceLinkageIntervalMethodPartitionReplicationDeploymentFixture(fixture);
        if (stable(compiled) !== stable(expected)) errors.push('compiled PC-48 build does not deterministically reconstruct from fixture');
      } catch (error) {
        errors.push(`compiled PC-48 reconstruction failed: ${error instanceof Error ? error.message : String(error)}`);
      }
    }
  }
  return errors;
}

export function renderPreferenceLinkageIntervalMethodPartitionReplicationDeploymentMarkdown(compiled) {
  const lines = [
    '# Preference linkage-interval method, partition, replication, and deployment assurance',
    '',
    `**Status:** ${compiled.status}`,
    '',
    `**Worlds:** ${compiled.metrics.worlds}`,
    '',
    `**Complete method-assurance worlds:** ${compiled.metrics.complete_method_assurance_worlds}`,
    '',
    '## Frozen public surface',
    ''
  ];
  for (const [key, value] of Object.entries(compiled.baseline)) lines.push(`- ${key}: ${value}`);
  lines.push('', '## Bounded synthetic burdens', '');
  for (const [key, value] of Object.entries(compiled.metrics)) lines.push(`- ${key}: ${value}`);
  lines.push('', '## Claim boundary', '', 'This synthetic control does not establish a real executed implementation, deterministic runtime, leakage-free partition, exchangeability finding, independent replication, deployment-validity claim, causal effect, graph fact, allegation, or public-authority verdict.');
  return `${lines.join('\n')}\n`;
}
