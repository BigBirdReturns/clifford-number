import { readFileSync } from 'node:fs';import { validatePreferencePopulationFrameAssuranceBuild } from './lib/preference-population-frame-assurance.mjs';const path=process.argv[2]??'build/research/preference-population-frame-assurance.json';const errors=validatePreferencePopulationFrameAssuranceBuild(JSON.parse(readFileSync(path,'utf8')));if(errors.length){console.error(errors.join('\n'));process.exit(1);}console.log('preference population-frame assurance validation: PASS');
