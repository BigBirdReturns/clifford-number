#!/usr/bin/env node
import path from 'node:path';
import { fileURLToPath } from 'node:url';
import { ROOT,loadData,computeManifest,buildSummary,outputPaths,stable,write } from './lib/status-sovereignty-rd04-snap-source-availability-a02-io.mjs';
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#039;'}[c]));
export function generate(root=ROOT){
 const data=loadData(root),manifest=computeManifest(root),summary=buildSummary(data,manifest),paths=outputPaths();
 const topRows=summary.provisional_tied_rows.map(r=>`<tr><td><code>${r.state}</code> ${esc(r.state_name)}</td><td>${Object.values(r.dimension_scores).join(' · ')}</td><td><b>${r.total_score}</b>/16</td></tr>`).join('');
 const distributionRows=Object.entries(summary.score_distribution).map(([score,count])=>`<tr><td>${score}/16</td><td>${count}</td></tr>`).join('');
 const html=`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="robots" content="noindex,nofollow"><title>SSC RD-04 provisional SNAP source catalogue</title><style>:root{background:#eeeae0;color:#181714;font-family:system-ui,sans-serif}body{max-width:1100px;margin:auto;padding:40px 24px 72px;line-height:1.5}h1{font-size:clamp(2rem,5vw,4.5rem);line-height:1}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(170px,1fr));gap:12px}.card,table,.boundary{background:#fffdf7;border:1px solid #c9c1b2;border-radius:12px}.card{padding:16px}.card b{display:block;font-size:2rem}table{width:100%;border-collapse:collapse}th,td{padding:9px;border-bottom:1px solid #ddd5c7;text-align:left}.state{font-weight:800;color:#8c300d}.boundary{border-left:6px solid #7c2920;padding:18px}code,pre{overflow-wrap:anywhere}</style></head><body><p><b>CLIFFORD NUMBER · SSC-H01 · PROVISIONAL SOURCE-CATALOGUE SCORE ONLY</b></p><h1>Five states provisionally tie in the recovered catalogue</h1><p class="state">50/50 STATES SCORED · 53 OFFICIAL RECEIPTS · 200 SEARCH SLOTS DECLARED · QUERY RECEIPTS: 0/200 PRESERVED · PROVISIONAL TOP SCORE 12/16 · CA CT KS KY WA PROVISIONALLY TIED · SELECTION GATE OPEN · 0 RESIDUAL CLOSURES · GRAPH NONE · PUBLICATION NONE</p><div class="grid"><div class="card"><b>50</b>states</div><div class="card"><b>53</b>official receipts</div><div class="card"><b>200</b>declared search slots</div><div class="card"><b>0</b>query receipts</div><div class="card"><b>5</b>provisionally tied</div><div class="card"><b>0</b>residual closures</div></div><h2>Provisional catalogue frontier</h2><p>D1–D8 are shown in fixed order. The scores are deterministic over the recovered official-source catalogue. They do not prove that every declared query was executed or preserved, and they do not measure policy quality.</p><table><thead><tr><th>State</th><th>D1–D8</th><th>Total</th></tr></thead><tbody>${topRows}</tbody></table><h2>Score distribution</h2><table><thead><tr><th>Score</th><th>States</th></tr></thead><tbody>${distributionRows}</tbody></table><p>The complete fifty-row catalogue score and computed rejected shortlist remain in the checked-in source shards and selection file. A final highest-coverage selection remains blocked until per-state query and result custody is recovered or a reviewed alternative protocol is entered.</p><h2>Authority boundary</h2><pre class="boundary">${esc(JSON.stringify(summary.boundaries,null,2))}</pre><p><code>release SHA-256: ${manifest.combined_sha256}</code></p></body></html>\n`;
 const outputs=new Map([[paths.manifest,stable(manifest)],[paths.buildManifest,stable(manifest)],[paths.buildSummary,stable(summary)],[paths.reportSummary,stable(summary)],[paths.html,html]]);return {data,manifest,summary,outputs};
}
export function build(root=ROOT){const generated=generate(root);for(const [rel,value] of generated.outputs)write(root,rel,value);console.log('build-status-sovereignty-rd04-snap-source-availability-a02: 50 states, provisional top 12, CA/CT/KS/KY/WA tied, selection gate open');return generated;}
if(process.argv[1]&&path.resolve(process.argv[1])===fileURLToPath(import.meta.url))build();
