name: Industrial-exhaust receipt helper attestation controller v1

on:
  push:
    branches:
      - agent/industrial-exhaust-receipt-helper-attestation-controller-v1
    paths:
      - .github/workflows/industrial-exhaust-receipt-helper-attestation-controller-v1.yml

permissions:
  contents: write

concurrency:
  group: industrial-exhaust-receipt-helper-attestation-controller-v1
  cancel-in-progress: false

jobs:
  qualify:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/industrial-exhaust-receipt-helper-attestation-controller-v1
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Reproduce and repair helper transcript custody
        shell: bash
        run: |
          set -euo pipefail

          BASE_SHA='952d5b3423492bee296cfc8e4d25c11060774f43'
          PRODUCT_BRANCH='agent/industrial-exhaust-receipt-helper-attestation-v1'
          WORKFLOW_PATH='.github/workflows/industrial-exhaust-receipt-helper-attestation-controller-v1.yml'
          TEST_PATCH='.github/tmp/industrial-exhaust-helper-attestation-test-patch.py'
          LIB_PATCH='.github/tmp/industrial-exhaust-helper-attestation-lib-patch.py'
          LIB_PATH='tools/lib/industrial-exhaust-artifacts.mjs'
          TEST_PATH='test/industrial-exhaust-retained-store-custody.test.js'

          current_sha="$(git rev-parse HEAD)"
          git merge-base --is-ancestor "$BASE_SHA" "$current_sha"

          expected_controller_paths=(
            "$LIB_PATCH"
            "$TEST_PATCH"
            "$WORKFLOW_PATH"
          )
          mapfile -t controller_diff < <(git diff --name-only "$BASE_SHA" "$current_sha")
          test "${#controller_diff[@]}" -eq "${#expected_controller_paths[@]}"
          for index in "${!expected_controller_paths[@]}"; do
            test "${controller_diff[$index]}" = "${expected_controller_paths[$index]}"
          done

          python -m py_compile "$TEST_PATCH" "$LIB_PATCH"
          python "$TEST_PATCH"

          if node "$TEST_PATH"; then
            echo 'predecessor unexpectedly accepted a forged helper transcript' >&2
            exit 1
          else
            echo 'predecessor helper-transcript custody defect reproduced'
          fi

          python "$LIB_PATCH"

          node --check "$LIB_PATH"
          node --check "$TEST_PATH"
          node "$TEST_PATH"
          node test/industrial-exhaust-artifacts.test.js
          node test/industrial-exhaust.test.js
          node tools/hydrate-industrial-exhaust.mjs --audit
          git diff --check

          rm "$WORKFLOW_PATH" "$TEST_PATCH" "$LIB_PATCH"

          mapfile -t product_diff < <(git diff --name-only "$BASE_SHA")
          test "${#product_diff[@]}" -eq 2
          test "${product_diff[0]}" = "$TEST_PATH"
          test "${product_diff[1]}" = "$LIB_PATH"

          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          tree_sha="$(git write-tree)"
          new_sha="$(
            printf '%s\n\n%s\n' \
              'fix(industrial-exhaust): attest helper-published receipts in the parent' \
              'Require an exact helper directory chain and independently open, hash, validate, and identity-check the visible final receipt before success.' \
              | git commit-tree "$tree_sha" -p "$BASE_SHA"
          )"

          mapfile -t final_diff < <(git diff --name-only "$BASE_SHA" "$new_sha")
          test "${#final_diff[@]}" -eq 2
          test "${final_diff[0]}" = "$TEST_PATH"
          test "${final_diff[1]}" = "$LIB_PATH"
          git diff --check "$BASE_SHA" "$new_sha"

          live_main="$(git ls-remote origin refs/heads/main | awk '{print $1}')"
          test "$live_main" = "$BASE_SHA"

          git push \
            --force-with-lease="refs/heads/${PRODUCT_BRANCH}:" \
            origin \
            "${new_sha}:refs/heads/${PRODUCT_BRANCH}"
