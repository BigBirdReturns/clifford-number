name: TEMP repair RD-04 North Dakota row-state schema instance conformance

on:
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - '.github/workflows/temp-rd04-nd-row-state-schema-instance-conformance.yml'
      - '.tmp/rd04-nd-row-state-schema-instance-conformance/materialize.py'
      - '.tmp/rd04-nd-row-state-schema-instance-conformance/trigger.json'

permissions:
  contents: write

env:
  CANONICAL_PARENT: 283c5993cb2ac0bff173c72aba70088c67b2ac3c
  SOURCE_HEAD: b41b72024bb9121fd865c8c6632b480ef2644bb8
  SOURCE_BRANCH: agent/ssc-rd04-nd-current-public-record-gap-row-state-reconciliation-v3
  TARGET_BRANCH: staging/ssc-rd04-nd-row-state-schema-instance-conformance-v1
  WORKFLOW_PATH: .github/workflows/temp-rd04-nd-row-state-schema-instance-conformance.yml
  SCRIPT_PATH: .tmp/rd04-nd-row-state-schema-instance-conformance/materialize.py
  TRIGGER_PATH: .tmp/rd04-nd-row-state-schema-instance-conformance/trigger.json
  OUT: /tmp/rd04-nd-row-state-schema-instance-conformance
  FULL_WORK: /tmp/rd04-nd-row-state-schema-instance-conformance-full
  QUAL_WORK: /tmp/rd04-nd-row-state-schema-instance-conformance-qual
  STAGE_WORK: /tmp/rd04-nd-row-state-schema-instance-conformance-stage

jobs:
  materialize:
    runs-on: ubuntu-24.04
    timeout-minutes: 240
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0

      - name: Initialize failure custody
        shell: bash
        env:
          EVENT_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          EVENT_BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          set -Eeuo pipefail
          rm -rf "$OUT" "$FULL_WORK" "$QUAL_WORK" "$STAGE_WORK"
          mkdir -p "$OUT"
          python - "$OUT/carrier-start.json" <<'PY'
          import json
          import os
          import pathlib
          import sys

          pathlib.Path(sys.argv[1]).write_text(
              json.dumps(
                  {
                      "schema_version": "ssc-rd04-nd-row-state-schema-instance-conformance-carrier-start@1",
                      "state": "carrier_started",
                      "event_head_sha": os.environ["EVENT_HEAD_SHA"],
                      "event_base_sha": os.environ["EVENT_BASE_SHA"],
                      "canonical_parent": os.environ["CANONICAL_PARENT"],
                      "source_head": os.environ["SOURCE_HEAD"],
                      "source_branch": os.environ["SOURCE_BRANCH"],
                      "target_branch": os.environ["TARGET_BRANCH"],
                  },
                  indent=2,
                  sort_keys=True,
              )
              + "\n",
              encoding="utf-8",
          )
          PY

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Repair, qualify, and stage exact product objects
        shell: bash
        env:
          EVENT_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
          EVENT_BASE_SHA: ${{ github.event.pull_request.base.sha }}
        run: |
          set -Eeuo pipefail
          python -m py_compile "$SCRIPT_PATH"
          python "$SCRIPT_PATH"

      - name: Preserve repair receipts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ssc-rd04-nd-row-state-schema-instance-conformance-v1
          path: ${{ env.OUT }}
          if-no-files-found: error
          include-hidden-files: true
          retention-days: 90
          compression-level: 9
