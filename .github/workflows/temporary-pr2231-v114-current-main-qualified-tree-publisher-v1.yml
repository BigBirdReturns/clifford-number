name: Temporary PR 2231 V114 current-main qualified-tree publisher placeholder

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  placeholder:
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'inert placeholder; publication disabled'
