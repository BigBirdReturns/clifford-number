name: Temporary materialize M-04F Wave 04 vertical joins

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  paused:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Payload repair in progress; final workflow will replace this pause.'
