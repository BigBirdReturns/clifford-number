name: Temporary PR 484 exact head export

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened]
    paths:
      - '.github/workflows/temporary-pr484-exact-head-export.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/pr484-exact-head-export
          fetch-depth: 0
      - name: Archive exact branch
        run: |
          set -euo pipefail
          mkdir -p /tmp/pr484-exact-head-export
          git archive --format=tar.gz --output=/tmp/pr484-exact-head-export/branch-head.tar.gz HEAD
          printf '%s\n' "$(git rev-parse HEAD)" > /tmp/pr484-exact-head-export/branch-head.txt
          sha256sum /tmp/pr484-exact-head-export/branch-head.tar.gz > /tmp/pr484-exact-head-export/archive.sha256
      - uses: actions/upload-artifact@v4
        with:
          name: pr484-exact-head-export-${{ github.run_id }}
          path: /tmp/pr484-exact-head-export/
          if-no-files-found: error
          retention-days: 2
