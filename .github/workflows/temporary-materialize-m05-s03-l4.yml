name: Temporary materialize M-05 Sprint 03 Leg 04

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/temporary-materialize-m05-s03-l4.yml'
  push:
    branches:
      - agent/m05-sprint-03-leg-04-workplace-co-governance
    paths:
      - '.github/workflows/temporary-materialize-m05-s03-l4.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/m05-sprint-03-leg-04-workplace-co-governance
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Build and validate workplace co-governance test
        run: |
          node tools/build-m05-answerable-power-sprint-03-leg-04.mjs
          node tools/validate-m05-answerable-power-sprint-03-leg-04.mjs
          node test/m05-answerable-power-sprint-03-leg-04.test.js
      - name: Commit generated products
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add reports/core-thesis/answerable-power/sprint-03-leg-04.json reports/core-thesis/answerable-power/sprint-03-leg-04.html
          if git diff --cached --quiet; then
            echo 'No generated drift'
          else
            git commit -m 'Materialize workplace co-governance stress-test report'
            git push origin HEAD:agent/m05-sprint-03-leg-04-workplace-co-governance
          fi
