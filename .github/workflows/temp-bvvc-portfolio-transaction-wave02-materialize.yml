name: TEMP materialize BVVC portfolio transaction wave 02

on:
  pull_request:
    paths:
      - '.github/workflows/temp-bvvc-portfolio-transaction-wave02-materialize.yml'
      - '.bvvc-portfolio-transaction-wave02/EXECUTE'
      - 'tools/build-bvvc-portfolio-transaction-wave02.mjs'
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: read

jobs:
  materialize:
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Build, qualify, and publish clean current-main product
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -Eeuo pipefail
          REPO="$GITHUB_REPOSITORY"
          TARGET='agent/bvvc-portfolio-transaction-census-wave02-product-v1'
          MAIN_SHA=$(gh api "repos/$REPO/git/ref/heads/main" --jq .object.sha)
          SOURCE_BUILDER="$GITHUB_WORKSPACE/tools/build-bvvc-portfolio-transaction-wave02.mjs"
          test -f "$SOURCE_BUILDER"

          rm -rf /tmp/bvvc-wave02-product
          git fetch --no-tags origin "$MAIN_SHA"
          git worktree add --detach /tmp/bvvc-wave02-product "$MAIN_SHA"
          cd /tmp/bvvc-wave02-product
          mkdir -p tools
          cp "$SOURCE_BUILDER" tools/build-bvvc-portfolio-transaction-wave02.mjs

          node tools/build-bvvc-portfolio-transaction-wave02.mjs
          node tools/validate-bvvc-defense-capital.mjs
          node test/person-routers.test.js
          npm run release:check

          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            data/intake/bvvc-defense-capital/README.md \
            data/intake/bvvc-defense-capital/coverage-matrix.json \
            data/intake/bvvc-defense-capital/manifest.json \
            data/intake/bvvc-defense-capital/portfolio-transaction-wave02-receipt.json \
            data/intake/bvvc-defense-capital/source-inventory-05.jsonl \
            data/intake/bvvc-defense-capital/transactions.jsonl \
            tools/build-bvvc-portfolio-transaction-wave02.mjs \
            tools/validate-bvvc-defense-capital.mjs
          test "$(git diff --cached --name-only | wc -l | tr -d ' ')" = '8'
          git commit -m 'Expand BVVC portfolio transaction census wave 02'
          PRODUCT_SHA=$(git rev-parse HEAD)

          START=$(git ls-remote origin "refs/heads/$TARGET" | awk '{print $1}')
          if [[ -n "$START" ]]; then
            git push origin "HEAD:refs/heads/$TARGET" --force-with-lease="refs/heads/$TARGET:$START"
          else
            git push origin "HEAD:refs/heads/$TARGET"
          fi

          mkdir -p /tmp/bvvc-wave02-receipt
          jq -n \
            --arg base_main "$MAIN_SHA" \
            --arg product_sha "$PRODUCT_SHA" \
            --arg target "$TARGET" \
            '{schema_version:"bvvc-portfolio-transaction-wave02-materialization-receipt@1",base_main:$base_main,product_sha:$product_sha,target_branch:$target,changed_paths:8,source_inventory_rows:79,transaction_rows:17,remaining_labels_without_transaction_or_explicit_investment_observation:13,private_support_rows:0,outside_human_dependency:false,ownership_findings:0,governance_right_findings:0,transaction_to_vehicle_joins:0,publication_effect:"none",adoption_effect:"none",graph_effect:"none"}' \
            > /tmp/bvvc-wave02-receipt/status.json
          sha256sum /tmp/bvvc-wave02-receipt/status.json > /tmp/bvvc-wave02-receipt/SHA256SUMS

      - uses: actions/upload-artifact@v4
        with:
          name: bvvc-portfolio-transaction-wave02-materialization-receipt
          path: /tmp/bvvc-wave02-receipt
          if-no-files-found: error
          retention-days: 90
