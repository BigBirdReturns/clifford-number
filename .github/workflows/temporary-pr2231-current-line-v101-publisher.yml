name: Retired PR 2231 current-line V101 publisher

on:
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/temporary-pr2231-current-line-v101-publisher.yml'

permissions:
  contents: read

jobs:
  retired:
    if: ${{ github.event.pull_request.number == 0 }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'V101 publisher retired after exact owner-origin publication'
