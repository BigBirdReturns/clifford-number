name: TEMP RD-01 Wave-03 workflow permission handoff

on:
  pull_request:
    paths:
      - '.github/workflows/temp-ssc-rd01-wave03-workflow-handoff.yml'
      - '.github/tmp/rd01-wave03-workflow-handoff.sh'
      - '.ssc-rd01-wave03-workflow-handoff/TRIGGER'
  workflow_dispatch:

permissions:
  contents: write
  actions: read
  pull-requests: write

concurrency:
  group: ssc-rd01-wave03-workflow-permission-handoff
  cancel-in-progress: false

jobs:
  handoff:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - id: handoff
        continue-on-error: true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: bash .github/tmp/rd01-wave03-workflow-handoff.sh
      - name: Preserve handoff receipt
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ssc-rd01-wave03-workflow-handoff
          path: /tmp/ssc-rd01-wave03-workflow-handoff-receipt
          if-no-files-found: error
          retention-days: 90
      - name: Enforce handoff result
        if: always()
        env:
          HANDOFF_OUTCOME: ${{ steps.handoff.outcome }}
        run: test "$HANDOFF_OUTCOME" = 'success'
