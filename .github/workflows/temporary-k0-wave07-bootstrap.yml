name: Temporary K0 Wave 07 exact-checkout bootstrap
# Refresh snapshot after carrier repair fragments were added.

on:
  push:
    branches: [agent/k0-role-neutral-wave-07]
    paths:
      - '.github/workflows/temporary-k0-wave07-bootstrap.yml'
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/temporary-k0-wave07-bootstrap.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/k0-role-neutral-wave-07
          fetch-depth: 0
      - name: Package exact checkout
        run: |
          set -euo pipefail
          git rev-parse HEAD > /tmp/HEAD_SHA
          git status --porcelain=v1 > /tmp/WORKTREE_STATUS
          tar --exclude=.git -czf /tmp/k0-wave07-checkout.tar.gz .
          sha256sum /tmp/k0-wave07-checkout.tar.gz > /tmp/CHECKOUT_SHA256
      - uses: actions/upload-artifact@v4
        with:
          name: k0-wave07-exact-checkout
          path: |
            /tmp/k0-wave07-checkout.tar.gz
            /tmp/HEAD_SHA
            /tmp/WORKTREE_STATUS
            /tmp/CHECKOUT_SHA256
          if-no-files-found: error
          retention-days: 3
