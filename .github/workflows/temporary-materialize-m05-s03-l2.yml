name: Temporary materialize M-05 Sprint 03 Leg 02

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/temporary-materialize-m05-s03-l2.yml'
  push:
    branches:
      - agent/m05-sprint-03-leg-02-administrative-benefits
    paths:
      - '.github/workflows/temporary-materialize-m05-s03-l2.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/m05-sprint-03-leg-02-administrative-benefits
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - name: Build and validate administrative-benefits test
        run: |
          node tools/build-m05-answerable-power-sprint-03-leg-02.mjs
          node tools/validate-m05-answerable-power-sprint-03-leg-02.mjs
          node test/m05-answerable-power-sprint-03-leg-02.test.js
      - name: Commit generated products
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add reports/core-thesis/answerable-power/sprint-03-leg-02.json reports/core-thesis/answerable-power/sprint-03-leg-02.html
          if git diff --cached --quiet; then
            echo 'No generated drift'
          else
            git commit -m 'Materialize administrative-benefits stress-test report'
            git push origin HEAD:agent/m05-sprint-03-leg-02-administrative-benefits
          fi
