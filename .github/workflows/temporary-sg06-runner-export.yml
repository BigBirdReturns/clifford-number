name: Temporary SG-06 runner export

on:
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/temporary-sg06-runner-export.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  export:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/publication-sg06-runner-export
          fetch-depth: 0
      - name: Archive exact runner branch
        run: |
          set -euo pipefail
          mkdir -p /tmp/sg06-runner-export
          git archive --format=tar.gz --output=/tmp/sg06-runner-export/branch-head.tar.gz HEAD
          printf '%s\n' "$(git rev-parse HEAD)" > /tmp/sg06-runner-export/branch-head.txt
          sha256sum /tmp/sg06-runner-export/branch-head.tar.gz > /tmp/sg06-runner-export/archive.sha256
      - uses: actions/upload-artifact@v4
        with:
          name: sg06-runner-export-${{ github.run_id }}
          path: /tmp/sg06-runner-export/
          if-no-files-found: error
          retention-days: 2
