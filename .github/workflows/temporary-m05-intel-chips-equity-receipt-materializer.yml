name: Retired temporary M-05 Intel CHIPS equity receipt materializer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Retired after qualified product publication moved to the review-repair controller.'
