name: Retired PR 2231 current-main V95 publisher

on:
  pull_request:
    branches:
      - main
    paths:
      - '.github/workflows/temporary-pr2231-current-main-v95-publisher.yml'

permissions:
  contents: read
  pull-requests: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo 'V95 publisher retired after exact publication receipt; no further product writes are authorized.'
