# Security-state organism evidence program

M-04B tests whether a post-2001 counterterrorism architecture was generalized into a modular transnational population-control architecture and whether nominally separate public and private actors performed complementary functions within it.

The program is an adversarial acquisition and falsification surface, not an organism finding.

```text
population and environment capture
→ identity resolution
→ classification and relationship inference
→ persistent tracking
→ decision, case, or target production
→ enforcement or effect
→ outcome feedback and adaptation
→ export, replication, and normalization
```

## Integrity repair

The canonical source plane consists of:

- `data/project/security-state-organism-program.json`;
- `data/project/security-state-entity-registry.json`;
- `data/project/security-state-estate-alignment.json`;
- `data/project/security-state-work-packages.json`;
- `data/intake/security-state-organism-source-routes.json`;
- `data/intake/security-state-organism-evidence-intake.json`.

The builder may project those files into packets, manifests, issue plans, and the public atlas. It may not create source routes, entity assignments, estate assignments, lineage targets, theater targets, evidence, or priority from array position, modulo arithmetic, round-robin selection, count balancing, or a desired visual shape.

An empty target array is meaningful. It records unresolved target selection and remains empty until a source record selects the target.

## Current bounded surface

```text
24 estate packets
15 functional-organ packets
8 lineage-stage packets
8 organism-test packets
7 theater packets
41 entity and alias packets
18 cross-estate bridge packets
121 total packets
```

The source-bounded intake begins with nine records. Each record separately states what the opened source supports, what it does not support, and the next acquisition needed. First-party statements remain first-party claims or methodology. Official public records remain bounded to the event, identity, date, or public gate they actually record.

## Operating law

Every packet must terminate in a typed state such as supported for human review, retained candidate only, bounded non-link, falsified, identity ambiguous, temporal order unresolved, source restricted, source unavailable, or requiring additional acquisition.

No packet count, workflow result, visualization, shared investor, shared employee, common customer, technology category, theater, chronology, policy vocabulary, or source route may generate organism membership, common design, coordination, coercion, extraction, wrongdoing, or estate completion.

```text
promotes_to: candidate_only
graph_effect: none
conclusion_generated: false
estate_completion_claimed: false
synthetic_assignment: false
```
