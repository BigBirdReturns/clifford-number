# Preference Custody laboratory floor v26

## Status

Floor v26 composes the qualified twenty-seven-control floor v25 with PC-28, topology measurement error, hidden-edge, and dynamic-exposure custody. It remains a synthetic laboratory floor with no graph effect, no thesis-evidence consumption, no named-actor finding, no real-world effect claim, and no public-authority conclusion.

## Composition

```text
base floor          preference-custody-laboratory-floor-v25
base controls       27
extension control   PC-28
final controls      28
new requirements    60
```

The compiler reconstructs every predecessor control from its source fixture, validates the complete v25 build, compiles PC-28, preserves the base control array and promotion boundary, admits the new control, transitions the topology frontier, and seals a new five-event floor chain.

## PC-28 publication surface

All eight PC-28 worlds preserve:

```text
operative release                RELEASE-INCIDENT-V1@1
observed nodes                   100
published node coverage          100%
published edges                 1000
published missing edges            0
published snapshots                3
public topology status           topology_verified
published stability coefficient  0.95
declared pre-treatment map       complete
approved use                     interference_adjusted_effect
```

The worlds distinguish complete assurance from identity collision and fragmentation, boundary truncation, differential edge censoring, structural collapse, stale nonconcurrent paths, endogenous rewiring, and unvalidated reconstruction.

## Frontier transition

Floor v26 resolves:

```text
network_topology_measurement_error_hidden_edge_and_dynamic_exposure_assurance
```

and replaces it with the narrower successors:

```text
identity_resolution_entity_boundary_and_network_frame_assurance

edge_ascertainment_multiplex_temporal_reconstruction_and_path_validity_governance
```

The independent frontier remains open:

```text
saturation_general_equilibrium_and_interference_robust_policy_governance
```

The transition is a refinement, not an assertion that either successor has been resolved.

## Added real-case custody

The sixty new machine-addressable requirements cover:

- observed records, true entities, aliases, collisions, fragmentation, false merges, false splits, confidence, correction, and identity succession;
- declared and operational boundaries, external nodes, bridging ties, alternate frames, boundary changes, exceptions, and succession;
- edge population, collection frame, ascertainment, missingness, censoring, direction, sign, weight, multiplex layers, hyperedges, shared contexts, confidence, audit, and falsification;
- snapshot identity, collection time, validity interval, temporal concurrency, ordered paths, pre-treatment status, staleness, and assignment-treatment-outcome timing;
- created, deleted, persisted, reverted, and endogenous rewired edges;
- reconstruction model identity, features, training data, calibration, support, validation sample, false positives, false negatives, replication, and observed-imputed crosswalks;
- path identity, direction, layer, boundary, hyperedge, shared-context, temporal feasibility, dose accumulation, decay, and misclassification;
- hidden-edge, bridge, placebo-edge, negative-control, alternative-topology, and sensitivity audits;
- approved and executed lineage, monitoring, refresh, drift, rollback, recomputation, correction, appeal, certificate withdrawal, public claim limitations, authority, and hash-linked interpretation custody.

## Floor integrity

A qualified v26 build must demonstrate:

```text
base floor qualified
base integrity preserved
all graph effects none
no thesis-evidence consumption
no real-world conclusion
no preference-change claim
no intent inference
all PC-28 refusal rules present
one complete topology-assurance path preserved
```

The base snapshot and extension snapshot are independently SHA-256 bound. The final promotion-requirement count must equal the base count plus exactly the newly admitted requirements.

## Five-event compositional chain

```text
qualified v25 floor snapshot
→ PC-28 admitted
→ topology frontier transition sealed
→ real-case promotion boundary sealed
→ interpretation sealed
```

## Claim boundary

Floor v26 establishes only that one public `topology_verified` surface can coexist with eight incompatible topology-provenance states and that real-case promotion requires complete custody. It does not establish any real network, edge, path, exposure, causal effect, policy effect, market effect, coercion, manipulation, discrimination, breach, misconduct, coordination, common purpose, intent, legitimacy, or public authority.

## Commands

```bash
node tools/compile-preference-topology-assurance.mjs
node tools/validate-preference-topology-assurance.mjs
node test/preference-topology-assurance.test.js

node tools/compile-preference-custody-manifest-v26.mjs
node tools/validate-preference-custody-manifest-v26.mjs
node test/preference-custody-manifest-v26.test.js
```
