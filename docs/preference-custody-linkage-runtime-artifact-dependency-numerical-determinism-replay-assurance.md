# PC-49 runtime artifact, dependency, numerical determinism, and replay custody

PC-49 holds one complete-looking public linkage-interval artifact and replay surface constant across eight incompatible artifact, dependency, hardware, numerical, PRNG, parallelism, replay-equivalence, and lineage worlds.

The control is synthetic. It creates no real executable-artifact audit, dependency attestation, portability result, interval-validity finding, effect, graph fact, allegation, or authority verdict.

## Frozen surface

```text
candidate pairs                 100
nominal / empirical coverage    95% / 95%
public replay runs              10 / 10 matching
artifact status                 artifact_digest_verified
replay status                   deterministic_replay_verified
portability status              portable_replay_verified
approved use                    longitudinal_exposure_estimation
```

## Qualified distinction

Only the complete world binds the executed artifact, dependency graph, environment, hardware, precision, PRNG state and draw order, parallel execution order, pair-level equivalence, divergence disposition, and current lineage. The other seven worlds preserve the same public badges while breaking one of those custody surfaces.

## Boundary

A real deterministic or portable replay claim still requires exact current artifact, dependency, environment, hardware, numerical, randomness, parallelism, input, output, tolerance, divergence, correction, and succession receipts. No outside-human dependency is permitted.
