# SSC RD Wave 03 RD-04 postpromotion next-route protocol and execution-control repair

The canonical protocol froze the first route denominator selected after the four-cell promotion. Its five URLs cover the minimum open substantive frontier in Montana and North Dakota without changing any source, field, row, class, cumulative-ledger, publication, adoption, or graph state.

A single five-route transport capture later completed under canonical merge `91b386df809079884825b19a6b5d864b6e739172`:

```text
workflow run / artifact: 31244816987 / 9018149945
artifact SHA-256: 9b828cef364b156cc995409e445d5a1250c981127d21b841d56b05ef14b036e3
logical route attempts: 5
physical HTTP requests: 5
redirect requests: 0
HTTPS requested and final URLs: 5 / 5
source admissions: 0
field classifications: 0
row-state mutations: 0
class closed: false
```

The post-merge review identified five execution-control defects: missing immutable-input trigger paths, mutable root selection, redirect requests outside the total-request accounting, possible same-host HTTPS-to-HTTP downgrade, and an embedded receipt hash that identified a preimage rather than the final retained receipt bytes.

This repair pays all five controls without changing the selected states, fields, route IDs, URLs, or source-admission boundary. The standing workflow now runs when any of the six immutable inputs changes and validates ordinary same-repository pull requests without applying the historical one-commit product-topology assertion. The runner accepts no root override, binds the route ledger and contract by SHA-256, counts initial and redirect requests against one five-request physical budget, refuses non-HTTPS redirect targets, and records final receipt hashes only in an external receipt-file ledger and `SHA256SUMS`.

Additional route execution is blocked. The retained capture remains transport custody until a separate permanent adjudication authenticates the artifact, records the nonmanifestation of the first four defects in that exact run, replaces the misleading embedded receipt-hash interpretation with explicit preimage and final-file custody, and then evaluates the captured bodies offline.
