# SSC RD‑02 Wave 03 portfolio-lifecycle intake protocol

This object freezes the public-record protocol for residual class `RD-02-C05` without executing acquisition or changing the cumulative ledger.

```text
class:                    RD-02-C05
closure target:           complete portfolio investment, follow-on, exit,
                          write-off, default, return, and repayment ledger
frozen fund rows:         18
public / withheld rows:   17 / 1
required fields per row:  10
required matrix cells:    180
materialized cells:       0
terminal cells:           0
class closed:             false
```

## Parent custody

The exact row order and legal-vehicle labels are inherited from the terminal `RD-02-C04` license-and-leverage matrix. That parent class remains closed and is not reopened, rewritten, or counted again.

The withheld eighteenth row remains an affirmative denominator member. No name may be guessed, substituted, or inferred from search results.

## Fixed route denominator

The route set is fixed before any response is observed:

```text
exact official GET routes:      6
fixed Bing RSS census routes: 136
fixed routes total:           142
maximum attempts per route:     1
result-spawned requests:         0
automatic candidate follow-up: false
automatic second pass:         false
```

The six exact official routes preserve the first-cohort identity record, cohort PDF, SBICCT programme surface, later SBIC directory, portfolio-reporting and leverage-repayment rules, and official resource library.

Each of the seventeen public fund rows receives the same eight role-neutral query families:

```text
identity
portfolio
investment or financing
follow-on investment
exit or acquisition
write-off, loss, default, or cure
return or distribution
SBA leverage repayment, debenture, or liquidation
```

The protocol therefore does not search more deeply for a fund because an early result appears promising or adverse.

## Candidate law

Search responses create only a frozen candidate census. They do not establish a portfolio company, financing, follow-on, exit, write-off, default, cure, realized return, SBA repayment, public recovery, or fund identity.

```text
search result ≠ evidence
official domain ≠ substantive support
first-party domain ≠ substantive support
lexical legal-vehicle match ≠ identity resolution
search rank ≠ authority
nonreturn ≠ record absence
```

Any URL follow-up requires a separate frozen successor denominator. Evidence admission then requires page-level custody tying the exact fund vehicle or unambiguous manager/fund lineage to a dated lifecycle event and the relevant investment or repayment instrument.

## Next exact action

Execute all 142 routes once, preserve request metadata, response headers, bodies, terminal states, byte counts, and SHA-256 values, and parse the RSS responses into candidate rows without following them. The resulting candidate census must then be frozen before any page-level adjudication begins.

## Authority ceiling

```text
outside-human dependency:  false
external contacts/reviews: 0 / 0
capital-conversion finding: false
favoritism finding:        false
extraction finding:        false
coordination/common-purpose:false / false
publication/adoption/graph:none / none / none
```

A projected programme total is not a fund investment. An investment is not a follow-on or exit. An exit is not necessarily a positive realized return. Missing public loss or default records are not proof of no loss or default. A private distribution is not SBA repayment or public recovery.
