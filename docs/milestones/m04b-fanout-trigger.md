# M-04B fan-out trigger

This trigger exists only to verify that the complete adversarial evidence program has been published on the branch. It is removed before merge if the permanent branch already contains the same state.
