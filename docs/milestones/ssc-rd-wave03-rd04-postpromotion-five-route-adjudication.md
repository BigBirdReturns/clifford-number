# SSC RD Wave 03 RD-04 postpromotion five-route adjudication

## Object classification

This product is an offline source-and-field adjudication over the five unique bodies captured by the canonical postpromotion protocol. It preserves ten HTTP observations from two complete executions, deduplicates substantive weight by body SHA-256, visually reviews all eight Montana PDF pages, classifies four captured objects as narrow field-review sources, rejects the North Dakota manual shell as field evidence, records six field decisions, freezes four bounded promotion candidates, and freezes two exact North Dakota follow-up routes.

It does not update the fifty-state matrix, terminalize a field, mutate a row state, close RD-04-C02, change the cumulative ledger, execute either follow-up route, or create publication, adoption, graph, national-prevalence, discrimination, racial-order, coordination, common-purpose, or complete-compact authority.

## Canonical input and transport custody

```text
canonical protocol merge:
91b386df809079884825b19a6b5d864b6e739172

canonical protocol tree:
7825098153b33c15418c973d1d04ff22eb50e015

successful execution PRs:             1501 / 1503
successful workflow runs:   31244816987 / 31245068161
successful artifacts:             9018149945 / 9018224489
successful HTTP attempts:                          10
unique routes:                                      5
unique body identities:                             5
body changes between runs:                          0

zero-request failure PR:                         1502
zero-request failure run:                 31244867615
zero-request failure artifact:               9018165314
```

The two successful artifacts have different response `Date` windows and therefore represent distinct network transactions. Every route nevertheless has the same byte count, ETag where present, last-modified value where present, body SHA-256, and normalized response-receipt SHA-256 in both runs. The product preserves all ten observations but grants substantive weight once per unique body identity.

## Narrow source decisions

```text
unique body decisions:                              5
narrow source admissions:                           4
field-review sources:                               4
nonadmitted runtime shells:                         1
PDF sources:                                        3
HTML sources:                                       2
```

The three Montana PDFs are exact first-party public policy documents. Every page was rendered and visually reviewed. North Dakota section 403 is an exact first-party policy section. The North Dakota manual home returned an 887-byte script-only shell with no title and zero visible text characters, so it retains transport identity but supplies no field evidence.

## Six field decisions

The bounded-complete candidate decisions are:

```text
MT  operative_state_implementation_authority_and_version
MT  implementation_effective_date_or_typed_gap
MT  abawd_or_work_requirement_waiver_state_and_governing_period
ND  implementation_effective_date_or_typed_gap
```

The held decisions are:

```text
ND  operative_state_implementation_authority_and_version
    no_relevant_support_hold_open

ND  abawd_or_work_requirement_waiver_state_and_governing_period
    temporal_or_scope_ambiguity_hold_open
```

Montana's public index and sections support a bounded manual-authority finding, two section-specific effective dates, and the statement that no Montana area has an approved ABAWD geographic waiver as of November 1, 2025. North Dakota section 403 supports a bounded effective-date finding for that section. Its no-waiver text is limited to July 1 through October 31, 2025, while the page is effective November 1, 2025, so the current post-period waiver state remains open.

## Selected follow-up protocol

Two result-visible links are frozen without execution:

```text
https://www.nd.gov/dhs/policymanuals/SNAP/Content/Release%20Log.htm

https://www.nd.gov/dhs/policymanuals/SNAP/Content/History/403%20Geographic%20Waiver/403%20History%20Log.htm
```

Both exact and normalized forms are absent from the authenticated 18,606-exact and 18,597-normalized URL corpora exported from the protocol parent. A later separate never-merge carrier may issue at most one GET per route. This product issues zero requests.

## Authority ceiling

```text
source requests executed by adjudication:           0
source admissions created:                           4
field classifications created:                      0
field terminalizations created:                     0
matrix updates:                                      0
row-state mutations:                                 0
class closed:                                    false
cumulative-ledger effect:                          none
outside-human dependency:                         false
publication / adoption / graph:      none / none / none
```

Evidence tier: two authenticated execution artifacts, one authenticated zero-request failure artifact, ten per-route transport observations, five unique body identities, full-page PDF renders, captured HTML review, exact field-specific locators, exported URL-set exclusion custody, deterministic reconstruction, recursively exact schema validation, adversarial refusal tests, and complete repository gates. Venue: this permanent product, Issue #1017, and the later separate promotion or follow-up carriers. Target: source admission, bounded field adjudication, candidate selection, and follow-up selection only. Upside: four exact current cells can enter a separate promotion transaction while the two unsupported North Dakota cells remain explicit. Downside: the transport denominator was executed twice, and duplicate bodies cannot be counted as independent support. Failure mode: treating HTTP success, official hosting, duplicate capture, a historical no-waiver period, or a candidate decision as automatic matrix or row closure.

The control question is whether a separate promotion product can reauthenticate the exact current matrix and terminalize only the four bounded-complete candidate cells, while a separate two-request carrier resolves the two held North Dakota fields without widening either denominator.
