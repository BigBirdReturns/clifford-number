# Counter-Selector denominator seed

## Result

Wave 00 remains preserved as the protocol parent. `CS-W00-SUP-01` corrects its numeric-zero ambiguity without rewriting the historical object.

The first Counter-Selector estate is now a typed, source-routed denominator rather than an empty registry or a person-ranking exercise.

```text
program:                                      counter-selector-v1
phase:                                                  CS-W02
wave:                                               CS-W02-W01
source-routed objects:                                      30
unique K0 source records:                                   30
positive operator traces:                                    5
false-positive outsider controls:                            5
ordinary specialists:                                       5
high-status selected operators:                              5
repair-capable partnership traces:                           5
brittle or failed partnership traces:                        5
matched-control assignments:                                30
blind-first reviews:                                         0
support ledgers:                                             0
bounded tests:                                               0
contradiction tests:                                         0
handoff tests:                                               0
second-party reviews:                                        0
promotions:                                                  0
public person rankings:                                      0
graph effect:                                             none
publication:                                     staged_nonpublic
```

## What changed

The wave preserves the installed programme and candidate registry while adding:

- a strict candidate-record JSON Schema;
- a blind-first and support-aware review contract;
- six balanced denominator classes with five unique objects each;
- source routes into already retained K0 public records;
- one explicit matched control for every object;
- alternative explanations, counterevidence, falsifiers, and missing-receipt ledgers;
- deterministic JSON and HTML reports;
- an exact-byte release manifest;
- adversarial mutation tests; and
- path-scoped CI plus the complete repository release gate.

## Evidence ceiling

Every object remains `retained_candidate_only`.

```text
K0 source route ≠ operator capability finding
class assignment ≠ disposition
positive trace ≠ selected operator
negative control ≠ false person
formal role ≠ competence
low legibility ≠ hidden genius
policy architecture ≠ observed repair
conflict ≠ brittle partnership
source-routed intake ≠ blind review
source-routed intake ≠ field test
exact bytes ≠ evidence truth
```

No K0 row is promoted into an event, person edge, motive finding, universal-capability claim, employment decision, funding decision, publication clearance, or public ranking.

## Next empirical gate

The next admissible transition is not more naming. It is second-party review of identity-minimized artifact packets, followed by three consented `CS-FT-01` tests in unrelated domains. Every reversal, failure, withdrawal, custody break, reviewer disagreement, and class reassignment remains in the permanent denominator.
