# SSC RD Wave 03 · RD-04 source-census candidate adjudication

The fixed 204-route source census is complete and its complete 1,500-row candidate denominator is terminally adjudicated without browsing from a result.

```text
state rows:                                 50
required matrix cells:                     450
fixed / terminal source-census routes: 204 / 204
candidate-census routes:                   150
candidate rows:                           1500
unique candidate URLs:                     500
```

## Candidate result

Every captured candidate was tested against the same frozen SNAP-domain term battery over the lowercase concatenation of title, snippet, and URL.

```text
responsive SNAP-domain candidates:             0
official .gov but nonresponsive candidates:  164
nonofficial nonresponsive candidates:        1336
terminal candidate rows:                1500 / 1500
candidate followups selected:                    0
candidate sources admitted:                      0
field classifications created:                   0
```

Generic state portals, geography pages, travel pages, encyclopedias, news homepages, and lexical collisions are not silently promoted into state implementation, operative waiver, exemption use, or screening practice. Their terminal disposition is route nonresponsiveness, not proof that the relevant state record or practice does not exist.

## Exact official redirect replay

All 54 exact official FNS routes returned HTTP 200 only after redirecting to one unapproved final host:

```text
raw host:             www.fns.usda.gov
redirected host:      www.fna.usda.gov
source routes:                         54
original protocol admissions:          0
fixed replay routes:                   54
```

The replay protocol preserves each raw FNS identity, exact redirected path, prior body and header hashes, and one direct one-attempt transport URL at `www.fna.usda.gov`. A successful replay remains pending separate response-body adjudication; HTTP success does not automatically admit a source or classify a field.

## Authority boundary

```text
field matrix terminal:                 false
class state:                       still_open
class closed:                          false
outside-human dependency:              false
external contacts / reviews:           0 / 0
unlawful-discrimination finding:       false
racial-hierarchy finding:              false
national-prevalence finding:           false
coordination / common purpose:         false / false
complete-compact finding:              false
publication / adoption / graph:        none / none / none
```

The next operation is exact execution of the 54-route official redirect replay, followed by source-level and field-level adjudication. No result may spawn another request automatically.
