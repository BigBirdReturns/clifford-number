# Allocator-war targeted closure execution Wave 26

Wave 26 consumes the exact forty-task denominator produced by Wave 25. It executes only the thirty-six tasks whose source state is `ready_for_targeted_acquisition` and preserves the four tasks whose source state is `blocked_pending_gate_identification`.

Each executed task must have exactly one task plan. The plan must identify one or more source receipts and retain a coverage statement, included rows, unavailable rows, refused substitutions, a negative-search statement, correction routes, and a correction outcome. The permitted terminal states are `complete`, `partial`, `contradicted`, `no_qualifying_gate`, `unavailable_after_search`, and `outside_bounded_window`.

A task result is an acquisition record rather than an evidence row. `Partial` does not close a denominator. `Unavailable_after_search` is not a null. A shared source does not create a relationship, and recurrence does not establish prevalence. No execution result may create an identity, participation row, active claim, hop edge, finding, graph effect, or publication authority.

Gate-first queues use sequential authority. The public-interest G0 task identifies a bounded federal employment, contracting, grant, and funding-recipient selector through source-addressed executive and implementing instruments. Its two downstream tasks become `unblocked_for_next_wave`, but Wave 26 does not execute them. The legislative and political-finance G0 task terminates as `no_qualifying_gate` because the bounded research and public-record source set does not supply a source-complete selector, executed decision roster, material consequence, and correction chain. Its downstream tasks remain blocked.

The permanent source-plan builder derives the ready-task denominator from Wave 25 and reuses the exact inherited Wave 24 source registry entries referenced by those tasks. It assigns one explicit acquisition state to each ready task, retains the four blocked tasks outside the plan, and refuses to invent new source locators or source-byte custody. The execution builder then projects one result ledger per estate. Each ledger preserves the Wave 25 task, authority class, source state, result state, exact source references, negative findings, correction routes, and prohibited inferences.

The validator reconciles all eleven source queues, forty source tasks, thirty-six task plans, four blocked transitions, the exact inherited source registry, result-ledger hashes, lake basin membership, authoritative roots, generator contracts, graph digests, and release-gate registration.

The control question is whether every claimed closure can be reconstructed from the declared source receipts while every missing, blocked, unavailable, and no-gate state remains visible and non-promotional.
